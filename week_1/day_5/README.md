# День 5. Версии моделей

**Стадия:** Done
**Ветка:** `day5-models`

## Постановка задания

Выполнить один и тот же запрос на слабой, средней и сильной модели (в условии как пример —
«из начала, середины и конца списка HuggingFace»). Замерить время ответа, количество токенов и
стоимость. Сравнить качество ответов, скорость и ресурсоёмкость.

Результат — короткий вывод о различиях между моделями плюс ссылки. Формат сдачи — видео и код.

## Вопрос дня

> Реши логическую задачу:
> На улице стоят пять складов.
> Каменщик работает в зеленом складе.
> У портного есть леопард.
> На желтом складе едят котлету.
> Электрик ест хлеб.
> Желтый склад стоит сразу справа от синего склада.
> Тот, кто пьет коньяк разводит пуму.
> В белом складе пьют вино.
> В центральном складе едят пельмени.
> Плотник работает на первом складе.
> Сосед того, кто пьет водку, держит медведя.
> На складе по соседству с тем, в котором держат тигра, пьют вино.
> Тот, кто пьет виски, ест макароны.
> Программист пьет ром.
> Плотник работает рядом с красным складом.
> Кто ест пиццу?
>
> Первым словом ответа напиши профессию.

Разновидность загадки Эйнштейна: пять складов в ряд, у каждого свой цвет, работник,
напиток, еда и зверь. Четырнадцать подсказок задают всё однозначно.

**Подвох в самом вопросе: пиццы среди подсказок нет вовсе.** Названы котлета, хлеб, пельмени и
макароны — четыре еды из пяти, а пицца та самая пятая, о которой не сказано ничего. Найти её
можно только решив задачу целиком: расставить всё остальное, и пицца окажется в единственной
оставшейся клетке. Ровно так же в исходной загадке ищут владельца рыбки.

**Верный ответ — «плотник».** Решение единственное, проверено полным перебором всех
перестановок:

| склад | цвет | профессия | напиток | еда | зверь |
|---|---|---|---|---|---|
| 1 | белый | **плотник** | вино | **пицца** | медведь |
| 2 | красный | электрик | водка | хлеб | тигр |
| 3 | зелёный | каменщик | коньяк | пельмени | пума |
| 4 | синий | портной | виски | макароны | леопард |
| 5 | жёлтый | программист | ром | котлета | пятый |

Текст задачи приведён дословно, добавлены только переводы строк между подсказками: в исходной
записи точки шли без пробелов и подсказки слипались в одну строку.

**Последняя строка — единственная добавка к задаче**, и сделана она ради зрителя видео: ответ
виден с одного взгляда, без вчитывания в таблицу разбора.

### Разметка автоматическая

Вердикт достаётся из ответа программой (`models/cells.py`, `extract_verdict`) и сверяется с
верным. Правило: **первое слово ответа**, если это одна из пяти профессий.

Именно первое слово, а не первое встреченное в тексте, и это решение важное. Задача решается
таблицей, и модель почти наверняка распишет все пять профессий по складам, прежде чем дать
вывод. Правило «первое встреченное» вернуло бы ту профессию, которая раньше попалась в разборе,
то есть случайную, — и мы измеряли бы порядок изложения, а не ответ.

Ответ, начинающийся не с профессии, получает пустой вердикт. Это **не** ошибка клетки: она
остаётся успешной и полноправно участвует в замерах времени и денег. Пометь мы её сбоем, такая
клетка вылетела бы из замеров, и столбец модели, хуже прочих держащей форму, выглядел бы в
таблице самым быстрым и дешёвым — ошибка стирала бы саму себя.

## Какие модели взяли и почему

### Два разных списка

**Список HuggingFace.** В организации [`deepseek-ai`](https://huggingface.co/deepseek-ai) — 104
модели, и ступени по размеру там настоящие:

| | Модель | Параметров | Из них активны |
|---|---|---|---|
| начало списка | [`DeepSeek-R1-Distill-Qwen-1.5B`](https://huggingface.co/deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B) | 1,5 млрд | все |
| | `DeepSeek-R1-Distill-Qwen-7B`, `-Llama-8B` | 7–8 млрд | все |
| середина | `DeepSeek-R1-Distill-Qwen-14B`, `-32B` | 14–32 млрд | все |
| | `DeepSeek-R1-Distill-Llama-70B` | 70 млрд | все |
| | `DeepSeek-R1` | 671 млрд | 37 млрд |
| | **[`DeepSeek-V4-Flash`](https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash)** | 291 млрд | **13 млрд** |
| | [`DeepSeek-V4-Flash-Vision-Exp`](https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash-Vision-Exp) | 305 млрд | — |
| конец списка | **[`DeepSeek-V4-Pro`](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro)** | **1,6 трлн** | **49 млрд** |

**Список API.** [`GET /models`](https://api-docs.deepseek.com/) отдаёт ровно три:

| Модель API | Что это | Выложена как |
|---|---|---|
| `deepseek-v4-flash` | младшая | [`DeepSeek-V4-Flash-0731`](https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash-0731) |
| `deepseek-v4-pro` | старшая | [`DeepSeek-V4-Pro-0813`](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro-0813) |
| `deepseek-v4-flash-vision-exp` | flash плюс приём изображений, экспериментальная | [`DeepSeek-V4-Flash-Vision-Exp`](https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash-Vision-Exp) |

То есть **API отдаёт только верхушку списка HuggingFace**. Остальные 102 модели — открытые веса:
их скачивают и запускают у себя, по API DeepSeek они не отвечают. Требование условия «из начала,
середины и конца списка» по API недостижимо в принципе, и это первая находка дня.

Третью модель API в опыт не взяли: `flash-vision-exp` — не ступень качества, а тот же `flash` с
добавленным зрением. Поставить её посередине значило бы выдать за уровень то, что уровнем не
является, а картинок в нашем вопросе нет.

### Ступени: таблица 2×2 из двух моделей

Раз моделей две, а ступеней нужно три, вторая ось — **режим рассуждений**. У DeepSeek он включён
по умолчанию и выключается параметром `thinking: {"type": "disabled"}`. Те же веса, но модели
разрешено развернуть цепочку рассуждений перед ответом.

| | без рассуждений | с рассуждениями |
|---|---|---|
| **`flash`** | слабая | средняя |
| **`pro`** | контрольная | сильная |

Четвёртая клетка (`pro` без рассуждений) — не роскошь, а условие проверяемости. Без неё
утверждение «рассуждения тянут модель на класс выше» осталось бы предпосылкой опыта, а не его
результатом.

**Честная оговорка.** Это не три деления одной шкалы, а две разные оси: ёмкость весов и объём
вычислений при выводе. На задаче-ловушке, где узкое место — разворачивание рассуждения, порядок
скорее всего выстроится. На задачах, где узкое место — знания (фактология, стиль, нюанс), порядок
не гарантирован. Переносить вывод «включить рассуждения ≈ купить модель классом выше» на всё
подряд нельзя.

Разница в скорости при этом заложена в устройстве, а не в настройках: обе модели — смесь
экспертов, но на каждый токен `flash` считает 13 млрд параметров, а `pro` — 49 млрд.

### Отвергнутые пути

**Настоящая лестница по размеру через посредника** (OpenRouter, HuggingFace Inference Providers).
Дала бы буквально то, что просит условие: 1,5 млрд → 32 млрд → 671 млрд, причём часть моделей у
OpenRouter бесплатна (суффикс `:free`, 50 запросов в сутки без пополнения счёта). Отвергнута
решением: поставщик остаётся один. Цена отказа названа выше — ступени не по размеру моделей.

**Запуск малой модели на своём VPS.** Одно ядро и 961 МБ памяти: модель на 1,5 млрд параметров в
сжатом виде влезет и выдаст пару токенов в секунду, 7 млрд уже не влезет. Замер показал бы
нехватку железа, а не свойства модели.

## Что меряем

Одного «времени ответа» мало — оно смешивает три разные задержки. Показываем рядом:

- **время до первого токена** — что человек ощущает как «отвечает или висит»;
- **время до первого слова ответа** — у клеток с рассуждениями это совсем другое число;
- **полное время** и **токенов в секунду** — чистая скорость модели без сети.

Плюс расход: вход с разбивкой на попадание в промежуточное хранилище и промах, выход, **отдельно
токены рассуждений**, и посчитанная стоимость со снимком тарифа.

**Прогоны идут строго по одному.** В дне 4 клетки летели по четыре сразу — для температуры это
безразлично, для замера времени это порча данных: одновременные запросы дерутся и за наш
событийный цикл, и за очередь на стороне провайдера, и разброс начинает выглядеть свойством
модели. Первым в каждом нажатии идёт прогревочный вызов: он платит за установление соединения и
в статистику не идёт. Именно в каждом, а не один раз при запуске, — сервер может простоять час,
и прогретое соединение к моменту нажатия уже закрыто.

## Съёмка отчёта

Запись экрана начинается **прямо перед нажатием кнопки** прогона. После конца прогона нужно
переключить все четыре вида, задержавшись на каждом секунд на пять, и только потом остановить
запись.

Причина не в красоте: за один прогон собраны четыре разных измерения одной таблицы — ответ,
время, токены и деньги. Ролик с одним видом показывает четверть работы.

## Как запустить

```
cd week_1/day_5/test
PYTHONPATH=.. uv run --project .. python -m models.web
```

Страница — `http://127.0.0.1:8768/`. Порт слушается только на своей машине: у страницы нет
проверки, кто нажимает кнопку, а каждое нажатие — **пять** платных вызовов к API: четыре
клетки по одному прогону плюс прогрев соединения. Второй одновременный прогон сервер отклоняет —
кнопка защищает одну вкладку, а платит за все и держит порядок запросов сервер.

Запуск из песочницы `test/` — чтобы не давать работающей программе доступа к остальному коду
репозитория; `PYTHONPATH=..` нужен, потому что `uv run --project` выбирает окружение, но каталог
проекта в пути поиска модулей не добавляет.

Ключ доступа берётся из настроек `myharness` (`~/.config/myharness/config.json`); если ключа нет,
страница скажет об этом отдельным сообщением. В репозиторий ключ не попадает.

Проверки: `cd week_1/day_5 && uv run pytest` — 132 штуки, все без сети и без экрана.
Поведение страницы: `node tests/проба_страницы.mjs` — 86 проверок без браузера и без обращений
к API. Что этими пробами **не** проверяется, перечислено в `tests/сценарий_страницы.md`.

## Что где лежит

| Путь | Что это |
|---|---|
| `models/cells.py` | четыре клетки опыта, вопрос дня, параметры запроса |
| `models/pricing.py` | тариф DeepSeek, пиковые часы, снимок тарифа, расчёт стоимости |
| `models/run.py` | прогон клетки, три величины времени, журнал, последовательный обход |
| `models/summary.py` | сводка из журнала: медианы, разброс, токены, деньги, отношения |
| `models/web.py` | приложение: страница, настройки, поток событий, сводка |
| `models/page.html` | страница целиком: разметка, оформление, поведение |
| `data/runs.jsonl` | сырые записи прогонов, строка на вызов |
| `data/архив/` | журнал и разбор прежнего вопроса дня (про мойку машины) |
| `tests/` | 132 проверки на Python и проба страницы на 86 проверок |
| `дизайн.md`, `план.md` | замысел и шаги работы |

## Ход работы

### Research

Разобрано, какие модели вообще доступны, и в чём разница между двумя списками — тем, что отдаёт
API, и тем, что лежит на HuggingFace. Опрошены четыре профильных агента: по промптингу (как мерить
качество на вопросе-ловушке и портит ли опыт инструкция о формате), по эксплуатации (что считать
честным замером времени и стоимости, что писать в журнал), по архитектурам (насколько честно
подменять размер модели режимом рассуждений), по инструментарию Claude (как замерить Opus 5 без
ключа Anthropic).

Дизайн — `дизайн.md`. Отвергнутые подходы и их цена записаны там же.

### Plan

Шестнадцать шагов, план — `план.md`. Строгий цикл «тест → падение → код → проход» применён к
расчёту денег, замеру времени, журналу и сводке: ошибка там не видна глазами — неверная цена
отличается от верной только цифрой. Страница проверяется прогоном по записанному сценарию.

### Executing

Шаги выполнялись агентами-исполнителями, каждый шаг проверял отдельный агент. Проверки писались
до кода и **испытывались подменами**: в черновое решение вносилась ошибка, и проверялось, что
её ловит именно та проверка, ради которой она написана. По сводке так поймано одиннадцать подмен
из одиннадцати.

Что нашла раздельная проверка и не нашли бы сами проверки — код их проходил:

- **расчёт молча терял деньги.** Если бы DeepSeek прислал только одну половину разбивки по
  промежуточному хранилищу, вход считался бы недосчитанным в сто двадцать шесть раз. Причём
  ошибка уходила в опасную сторону — в занижение счёта;
- **ненаступивший замер писался нулём со статусом «успешно».** Прогон, где не пришло ни одного
  слова ответа, ложился в журнал как удачный с «время до первого слова — 0 мс», и сводка
  усреднила бы эти нули, занизив главную величину дня;
- **чтение журнала падало на камере.** Журнал пишется русскими буквами (чтобы читался глазами), а
  русская буква занимает два байта. Страница читает журнал раз в три секунды, пока прогон его
  дописывает; попадание между двумя байтами одной буквы роняло разбор **целиком**, вместе со всем
  уже собранным. На настоящей записи роняли 22% обрывов. Проверка на оборванную строку в наборе
  была — но резала латиницу, где каждая буква ровно один байт;
- **не-словарь в поле расхода ронял весь опыт**, теряя все оставшиеся оплаченные клетки;
- **цена на экране расходилась с ценой в журнале** вдвое у клетки, начатой до границы пикового
  часа и законченной после: снимок тарифа брался в двух местах по разным моментам.

## Прежний вопрос дня и что он показал

До логической задачи тот же опыт был поставлен на другом вопросе — про мойку машины: человек
дома, машина на подземной парковке, до мойки сто метров пешком либо четыре километра на машине
через развязку в больших пробках; как лучше добраться. Верный ответ — на машине: моют саму
машину, и без неё на мойке делать нечего.

Вопрос заменён на более трудный, но собранное сохранено: журнал в `data/архив/мойка-машины.jsonl`,
разбор — в `data/архив/мойка-машины-вывод.md`. Прогон стоил 0,054 доллара.

### Разметка: 16 ответов, четыре прогона на клетку

| клетка | верных |
|---|---|
| `flash` без рассуждений | **1 из 4** |
| `flash` с рассуждениями | **4 из 4** |
| `pro` без рассуждений | **1 из 4** |
| `pro` с рассуждениями | **4 из 4** |

**Старшая модель без рассуждений ответила не лучше младшей без них.** Переход с `flash` на `pro`
не дал на этой задаче ничего, а включение рассуждений дало всё. Ради проверки этого и заводилась
четвёртая, контрольная клетка — и она ответила против наивного ожидания «слабая, средняя,
сильная».

Границы утверждения узкие, и назвать их обязательно: две конкретные модели, один вопрос, четыре
прогона на клетку. Это не утверждение про модели вообще и не про режим рассуждений вообще.
Правдоподобное объяснение есть — ёмкость весов отвечает скорее за знания, а рассуждения за число
шагов перед ответом, и вопрос был про шаги, — но объяснение это догадка, а не результат опыта.

### Как именно ошибались модели без рассуждений

Не по невнимательности. Обе клетки без рассуждений **добросовестно считали** и приходили к
неверному ответу через верную арифметику.

Слабейшая разобрала оба варианта по пунктам — спуск с девятнадцатого этажа, пробки, дорога
обратно, «сэкономите минимум час своей жизни», — затем **придумала услугу, которой в условии
нет** (будто сотрудники мойки сами пригонят машину с подземной парковки) и заключила:
«спускайтесь вниз и идите пешком».

Старшая без рассуждений проговаривала противоречие вслух («в контексте „помыть машину“ без машины
вы этого не сделаете») — и всё равно советовала пешком, предлагая «запланировать мойку, когда в
следующий раз куда-то поедете».

### Числа прежнего прогона

| клетка | верных | до первого слова ответа, медиана | выходных токенов | из них рассуждения | дороже слабой |
|---|---|---|---|---|---|
| `flash` без рассуждений | 1 из 4 | 1,2 с | 3 792 | 0 | 1,00× |
| `flash` с рассуждениями | 4 из 4 | **43,1 с** | 26 995 | 26 543 | **6,92×** |
| `pro` без рассуждений | 1 из 4 | 1,8 с | 2 293 | 0 | 1,85× |
| `pro` с рассуждениями | 4 из 4 | **41,9 с** | 10 575 | 10 083 | **8,19×** |

Что подтвердилось на живом API и осталось верным для нового вопроса:

**1. Токены рассуждений входят в выходные** и тарифицируются как выход — формулу стоимости
править не пришлось. Но пропорция чудовищна: у младшей модели 26 543 токена размышления против
452 токенов видимого текста, **пятьдесят девять к одному**.

**2. Включение рассуждений добавляет 79 токенов на входе.** Вопрос один и тот же, дословно, а
входных токенов 88 против 167 — и разница одинакова у обеих моделей. DeepSeek подмешивает в
запрос свою инструкцию, когда режим рассуждений включён. Мы её не писали и не видим, но платим за
неё. Находка возможна только потому, что в журнал кладётся сырой расход.

**3. Цена растёт немонотонно.** Включить рассуждения у `flash` (6,92×) вчетверо дороже, чем
перейти на `pro` (1,85×). Причина не в ставке, а в объёме.

**4. Две величины времени себя оправдали.** Первый токен приходит через полторы секунды, а первое
слово ответа — через сорок с лишним. Одним числом «время ответа» этого не увидеть, и человек у
экрана решит, что всё зависло.

**5. Разброс огромен.** На одном прогоне младшая модель с рассуждениями израсходовала 11 722
токена и думала 99 секунд, на другом — 4 873 и 42 секунды. Вдвое. Именно этот разброс, а не
статистика, задал число прогонов: три, а не пять.

## Результат прогона

Один прогон, четыре клетки плюс прогрев, **0,052 доллара**.

| клетка | ответ по существу | формат | выход | из них рассуждения | до первого слова ответа | полное | цена | к слабой |
|---|---|---|---|---|---|---|---|---|
| `flash` без рассуждений | **плотник — верно** | не соблюдён | 4 543 | 0 | 1,4 с | 29,2 с | 0,00302 | 1,00× |
| `flash` с рассуждениями | **плотник — верно** | соблюдён | 28 536 | 28 339 | **201,7 с** | 202,8 с | 0,01890 | **6,26×** |
| `pro` без рассуждений | **программист — неверно** | не соблюдён | 3 044 | 0 | 1,7 с | 41,7 с | 0,00618 | 2,04× |
| `pro` с рассуждениями | **плотник — верно** | соблюдён | 12 122 | 11 949 | **164,9 с** | 166,2 с | 0,02420 | **8,01×** |

Три клетки из четырёх решили задачу верно. Ошиблась **старшая модель без рассуждений** — и ошиблась
показательно: она зашла в противоречие, объявила задачу некорректной и всё равно назвала ответ.

> «Остаётся, что портной — 1-й. Но 1-й — плотник. Противоречие. […] Отсюда можно заключить: в
> условии есть **ошибка**, но если следовать буквально, то **пиццу ест программист**.»

Задача корректна, решение единственное (проверено полным перебором). Модель не справилась с
разбором и назвала виноватым условие. Это опаснее простой ошибки: ответ подан уверенно, с
обоснованием, и читателю, который не решал задачу сам, отличить его от верного нечем.

### Автоматическая разметка не сработала на половине клеток

Обе клетки **без** рассуждений начали ответ со вступления — «Для решения задачи составим таблицу»,
«Давай разберём эту задачу пошагово», — и потому остались без вердикта: правило берёт первое слово.

| клетка | первые слова ответа | вердикт извлечён |
|---|---|---|
| `flash` без рассуждений | «Для решения задачи составим таблицу…» | нет |
| `flash` с рассуждениями | «Плотник.» | да |
| `pro` без рассуждений | «Давай разберём эту задачу пошагово.» | нет |
| `pro` с рассуждениями | «Плотник ест пиццу.» | да |

Риск этот был назван **до** прогона, и прогон его подтвердил. Разметка по первому слову меряет
две вещи разом — правильность и послушность форме, — а разделить их автоматически нечем.
Правильность в таблице выше проставлена **чтением ответов**, и это единственное место дня, где
человек заменил измерение.

Важно, что провал формы не случаен, а разделил клетки ровно по той же черте, что и всё остальное:
**требование формы выполнили только клетки с рассуждениями**. Клетки без рассуждений начинают
писать сразу и вспоминают про инструкцию уже по ходу — `flash` без рассуждений, например, честно
пишет в середине разбора «Но в задаче спрашивают… первым словом ответа напиши профессию. Значит,
ответ: плотник», то есть инструкцию помнит, но выполнить её задним числом уже нельзя.

### Чем заплачено

| клетка | верно | дороже слабой | медленнее слабой |
|---|---|---|---|
| `flash` без рассуждений | да | 1,00× | 1,00× |
| `flash` с рассуждениями | да | 6,26× | **6,93×** |
| `pro` без рассуждений | **нет** | 2,04× | 1,43× |
| `pro` с рассуждениями | да | **8,01×** | 5,69× |

Самая дешёвая клетка ответила верно. Самая дорогая — тоже верно, но в восемь раз дороже и почти
в шесть раз дольше. А вот вдвое более дорогая `pro` без рассуждений оказалась **единственной
ошибшейся**: за двойную цену получен неверный ответ.

Расход на размышления по-прежнему подавляющий: у `flash` с рассуждениями 28 339 токенов
рассуждения против 197 токенов видимого ответа — **сто сорок четыре к одному**. Ответ при этом
самый короткий в таблице: 491 символ против почти одиннадцати тысяч у той же модели без
рассуждений.

**Три с половиной минуты молчания.** У клеток с рассуждениями первый токен приходит через
полторы секунды, а первое слово ответа — через 165 и 202 секунды. Человек у экрана, не знающий
этого, решит, что приложение зависло.

## Четвёртая модель: Claude Opus 5

Ключа Anthropic API нет ни локально, ни в `.env` на VPS, поэтому ответ получен прямо в сессии
Claude Code. Числа — настоящие, из журнала сессии
(`~/.claude/projects/<проект>/<сессия>.jsonl`), где Claude Code записывает расход каждого
сообщения.

**Ответ на вопрос дня:**

> На машине — иначе мыть будет нечего.
>
> Погода, пробки и 100 метров здесь ни при чём: это признаки задачи «как добраться до точки», а у
> вас задача другая — «доставить на мойку автомобиль». Машина не пассажир, она предмет работы.
> Придёте пешком — придёте без неё.
>
> Единственное, ради чего стоило бы дойти пешком, — это сходить заранее и спросить, есть ли
> очередь и работают ли они. Но саму мойку без машины не сделать.

**Расход сообщения, в котором дан ответ** (модель `claude-opus-5`, обычный режим):

| величина | значение |
|---|---|
| выходных токенов | 1225 |
| из них токенов размышления | 592 |
| видимых токенов | 633 |
| свежих входных токенов | 2 |
| записано в промежуточное хранилище (окно 1 час) | 6594 |
| прочитано из промежуточного хранилища | 108 447 |

**Стоимость по [тарифу Anthropic](https://www.anthropic.com/pricing)** ($5 за миллион входных,
$25 за миллион выходных; запись в промежуточное хранилище с окном в час — двойная ставка входа, чтение оттуда же —
десятая часть):

| статья | токенов | ставка за миллион | стоимость |
|---|---|---|---|
| выход | 1225 | $25 | $0,0306 |
| запись в хранилище | 6594 | $10 | $0,0659 |
| чтение из хранилища | 108 447 | $0,50 | $0,0542 |
| свежий вход | 2 | $5 | $0,00001 |
| **итого** | | | **$0,151** |

### Что здесь измерено, а что нет

**Сравнимы честно только выходные токены.** Они относятся ровно к тексту, который модель
написала, и стоят рядом с `completion_tokens` DeepSeek на равных.

**Входные токены с DeepSeek несравнимы.** У DeepSeek это 55 токенов вопроса, здесь — вся сессия
целиком, 115 тысяч токенов рабочего контекста. Полтора десятка центов — цена не ответа на вопрос
про мойку, а всего дня работы, накопленного в этом сообщении.

**Время ответа не измерено вовсе.** Промежуток между записями в журнале сессии включает работу
инструментов и временем генерации не является. Ставить сюда число значило бы выдумать его.

**Токены самого ответа — оценка, но не с потолка.** Сообщение содержало не только ответ: там же
была оговорка про отсутствие ключа. Зато из настоящего расхода выводится **измеренное** отношение
для русского текста: 633 видимых токена на 1078 символов = **0,587 токена на символ**. По нему
сам ответ (415 символов) — около **244 токенов**, то есть $0,0061 по ставке выхода. Это не
прикидка «символы делить на четыре», а отношение, снятое с настоящего токенизатора Claude на
нашем же тексте.

## Выводы

Границы утверждений здесь узкие, и назвать их надо первым делом: **две конкретные модели**
(`deepseek-v4-flash` и `deepseek-v4-pro`), **два вопроса**, **один-четыре прогона на клетку**.
Это не утверждения про модели вообще и не про режим рассуждений вообще. Ниже — то, что вышло на
нашей выборке, и выборка мала.

**1. Размер модели сам по себе ничего не решил ни на одном из двух вопросов.**

На логической задаче ошиблась ровно одна клетка — **старшая модель без рассуждений**; младшая
без рассуждений ту же задачу решила. На прежнем вопросе про мойку машины старшая без рассуждений
дала 1 верный ответ из 4 — ровно столько же, сколько младшая без них. Переход `flash` → `pro` в
обоих опытах не купил правильности, а стоил вдвое дороже.

**2. Дорого стоит не ставка, а объём.** Переход на старшую модель поднял цену вдвое. Включение
рассуждений — в шесть-восемь раз, потому что модель пишет десятки тысяч токенов размышления.
У `flash` с рассуждениями вышло сто сорок четыре токена рассуждения на каждый токен видимого
ответа.

**3. Рассуждения делают ответ короче, а не длиннее.** Клетка `flash` без рассуждений выдала
одиннадцать тысяч символов путаного разбора; она же с рассуждениями — 491 символ и точный ответ.
Думать про себя дешевле для читателя и дороже для кошелька.

**4. Модель без рассуждений может объявить корректную задачу ошибочной.** Старшая модель без
рассуждений запуталась в разборе, написала «в условии есть ошибка» — и всё равно назвала ответ,
уверенно и с обоснованием. Задача при этом корректна, решение единственное. Отличить такой ответ
от верного, не решив задачу самому, нечем.

**5. «Время ответа» — не одно число, а три.** Первый токен приходит через полторы секунды, первое
слово ответа — через три с половиной минуты. Одной величиной эту разницу не увидеть, а именно она
определяет, решит человек, что программа зависла, или нет.

**6. Автоматическая разметка меряет не то, что кажется.** Правило «вердикт из первого слова»
измеряет правильность и послушность форме вместе, и на нашем прогоне разделило клетки пополам:
требование формы выполнили только те, что с рассуждениями. Это само по себе наблюдение, но
правильность в отчёте пришлось проставлять чтением ответов.

Урок общее: **на неоднозначном вопросе автоматическая метрика не молчит, а врёт**. Это выяснилось
на выброшенном вопросе про веларий Колизея: разметка честно посчитала вердикты и выдала за
качество выбор трактовки — сильнейшая клетка дала самый знающий ответ и получила «неверно»,
потому что эталон был неверен. Неоднозначность вопроса при этом была видна в расходе токенов
раньше, чем в ответах: шесть минут и тридцать восемь тысяч токенов на один ответ — это модель
ходит по кругу, а не думает.

**7. Что делать на практике.** Проверять обе ручки, а не только ценник: взять модель постарше и
успокоиться оказалось худшим из четырёх решений в обоих опытах — те же или худшие ответы за
двойные деньги. Нужна ли на вашей задаче ручка рассуждений, выясняется таким же прогоном, а не
рассуждением о том, как устроены модели.

## Ссылки

- **Видео**: https://drive.google.com/file/d/1sR9RKmdjkexfMusZGrBZdhDMilFthxec/view?usp=drive_link
- **Код дня**: https://github.com/AlexanderKorytin/ai-challenge-2026/tree/day5-models/week_1/day_5

Ссылка ведёт на ветку дня целиком, а не на отдельную фиксацию: фиксация не может содержать ссылку
на саму себя, и снимок по хэшу показывал бы состояние на шаг раньше — без ссылок и без выводов.
Ветка `day5-models` после сдачи не меняется, поэтому это тот же снимок, но в законченном виде.
