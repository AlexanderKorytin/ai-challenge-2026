[project]
name = "myharnessfish"
version = "0.1.0"
description = "myharness ветки day2-fish-facts: агент поиска информации о рыбах"
readme = "README.md"
authors = [
    { name = "AlexandrKorytin", email = "koritin84@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "openai>=1.50",
    "prompt-toolkit>=3.0.47",
]

[project.scripts]
myharnessfish = "myharness:main"

# Имя дистрибутива в этой ветке отличается от main намеренно: так команда myharnessfish
# (код ветки) и команда myharness (код main) живут рядом, не вытесняя друг друга.
# Сам импортируемый пакет остаётся myharness.
[tool.uv.build-backend]
module-name = "myharness"

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
