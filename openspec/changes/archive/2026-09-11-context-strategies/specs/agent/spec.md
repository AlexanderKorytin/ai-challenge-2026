## ADDED Requirements

### Requirement: Агент применяет стратегию и отдаёт фактический выбор
Агент SHALL применять `context_strategy` при каждой сборке запроса и SHALL сохранять полную успешную историю отдельно от выбранного хвоста. Результат каждого обмена SHALL нести стратегию, число выбранных завершённых пар, число не отправленных, но сохранённых пар, имя ветви при его наличии и список фактически отправленных сообщений.

Не отправленные строгим окном пары SHALL NOT увеличивать числа дословно забытых или заменённых выжимкой пар.

#### Scenario: строгим окном исключены четыре пары
- **WHEN** Sliding Window выбрал четыре из восьми сохранённых пар
- **THEN** результат содержит `selected_pairs` 4 и `omitted_pairs` 4
- **AND** числа забытых и заменённых выжимкой пар равны нулю

#### Scenario: запрос из ветви B
- **WHEN** обмен проведён в ветви B
- **THEN** результат называет ветвь B и содержит фактический путь только этой ветви

### Requirement: Агент Sticky Facts завершает обмен вместе с обновлением фактов
Агент режима `facts` SHALL начинать основной обмен и отдельный обмен извлекателя без последовательного ожидания одного перед другим. Завершённым для очереди агента обмен SHALL считаться только после завершения обоих. Расход и ошибка извлекателя SHALL возвращаться в результате основного обмена отдельными полями и SHALL входить в накопленный расход этого агента.

Извлекатель SHALL работать без собственной истории и SHALL записывать свой прогон в общий журнал отдельным именем исполнителя и с тем же `run_id`, когда он задан.

#### Scenario: основной ответ пришёл первым
- **WHEN** основной ответ завершён, а извлечение фактов ещё идёт
- **THEN** следующий запрос этого агента не начат
- **AND** другие панели могут продолжать свои обмены

#### Scenario: оба обмена завершены
- **WHEN** основной ответ и извлечение завершены
- **THEN** результат содержит расход основного обмена и расход извлекателя раздельно
- **AND** накопленный расход агента содержит их сумму без повторного сложения

#### Scenario: извлекатель вернул ошибку
- **WHEN** основной ответ успешен, а извлекатель завершился ошибкой
- **THEN** успешная пара сохранена
- **AND** результат содержит ошибку фактов, а редакция словаря не изменилась

### Requirement: Агент Branching пополняет только активную ветвь
После успешного ответа агент Branching SHALL добавить одну завершённую пару к голове своей ветви. При ошибке или отмене голова SHALL остаться прежней. Два агента ветвей SHALL иметь независимую память пути и SHALL NOT разделять изменяемый список сообщений.

#### Scenario: ответ A и ответ B завершились одновременно
- **WHEN** два агента ветвей успешно завершили обмены в любом порядке
- **THEN** в каждом пути появилась только его пара
- **AND** общая контрольная точка осталась одной и неизменной

#### Scenario: обмен A не удался
- **WHEN** обмен ветви A завершился ошибкой
- **THEN** голова A не сдвинулась, а путь B не изменился
