# Состояние: автомат состояний задачи (день 13)

Род работы: **задание дня 13** (неделя 3, день 3; ветка `w3d3` — после правок инструмента в `main`).
Ведущая среда: **Claude Code**.

## Устные решения пользователя

- 2026-09-16. Видение: глобальный профиль «разработчик», заводится командой `/profile new`.
  Стадии как в нашем конвейере: исследование → дизайн (ведущий зовёт консилиум, выбирает лучшие
  решения, выносит на утверждение) → план на утверждение → исполнение → проверка → готово.
  Переходы как в нашем конвейере.
- 2026-09-16. Стадия исследования (`Research`) в конвейере профиля тоже нужна.

- 2026-09-16. Стадии профиля — ровно как в нашем конвейере: Research (с дизайном и консилиумом)
  → Plan | Executing; Plan → Executing; Executing → Validation | Research;
  Validation → Report | Executing | Research; Report → Done.

- 2026-09-16. Переход и шаг модель двигает **вызовом инструментов** (как у Claude Code/Codex),
  а не разметчиком и не блоком в ответе. Цена названа и принята: с `tools` рассуждения прошлых
  ходов уходят обратно в каждом запросе; переделка потока, истории, сжатия, счёта токенов.
  Смягчение: инструменты только у профиля с картой стадий и при открытой задаче.

- 2026-09-16. Автомат — общее устройство: включается у любого профиля с полем `стадии`, карта у
  каждого своя; «разработчик» — лишь первый такой профиль.

- 2026-09-16. Видео: автомат ведёт работу **сценариста** — экранизация романа Гарри Гаррисона
  «Стальная крыса». Профиль «сценарист» заводится `/profile new`, стадии — нашего конвейера
  (RESEARCH с консилиумом режиссёр / продюсер / знаток романа → PLAN поэпизодник → EXECUTING
  сцены → VALIDATION сверка с замыслом → REPORT → DONE). Профиль «разработчик» на видео не
  нужен.

## Итоги разбора (Research)

- Код: у агента нет вызова инструментов (`api.py`), `response_format` есть. Группа запускается
  `team.run(state, вопрос, ведущий)` — можно звать программно. Этап задачи — любое слово,
  карта переходов оставлена днём 11 на этот день (`workspace.py:42`).
- Документация DeepSeek (thinking_mode): с `tools` в запросе рассуждения ВСЕХ прошлых ходов
  обязаны уходить обратно в каждом запросе — рост входа на каждом ходу.
- Разборы сходятся: переход через утверждение применяет только явная команда человека;
  пауза — признак поверх этапа; карта переходов — в json профиля; ожидаемое действие —
  поля заголовка задачи (кто ждёт + что).
- Разборы расходятся: как модели предложить переход — вызов инструментов (как у образцов)
  против отдельного дешёвого запроса-разметчика.

## Стадия

Executing — дизайн утверждён 2026-09-16; план `docs/план-автомат-задачи.md` согласован 2026-09-16 («да»).

Сделано шагов: 1 (замер), 4, 2, 3, 5, 6, 7, 9, 8, 10 (ветка w3d3). Дальше — стадия Validation (по одному: общий файл проверок). Профили консилиума заведены.

## Итоги шага 1 — живой замер DeepSeek (`deepseek-v4-flash`, рассуждения включены), 2026-09-16

- Имя инструмента — только `^[a-zA-Z0-9_-]+$`: русское имя даёт 400. Инструменты называются
  `update_plan`, `move_stage`, `call_council`. Русские имена ДОВОДОВ сервер принимает.
- Вызов: `finish_reason = tool_calls`, `content` пустой; первый кусок несёт `id`, `type`,
  `function.name` и пустые доводы, дальше доводы идут кусками по `index`.
- Продолжение после результата инструмента пришло без рассуждений (0 знаков) — пустые
  рассуждения круга законны.
- Сервер НЕ отказывает: старая пара без `reasoning_content` при `tools`; пустая строка;
  прошлый ход с вызовом без рассуждений; запрос без `tools`, в истории которого вызов.
  Решение для `раскрыть(полные=True)`: рассуждения кладутся, когда они записаны; у пары без
  них ключ не ставится. Пауза (снятие `tools`) не ломает разговор.
- Шум `generator didn't stop after athrow()` — известный дефект httpcore2, в `api.aclose`
  уже обойдён; к делу не относится.
- Отступ от плана: ключ карты в json профиля — `stages` (поля `name`, `goal`, `approval`,
  `council`, `next`), как все ключи json профиля; имена в Python русские.

## Открытые вопросы

- Вторая разведка сделана: вызовы и рассуждения ложатся полем записи ответа, пара сохраняется (Р8 дизайна).

## Решения ведущего внутри шагов

- Шаг 4, просмотр: неизвестный профиль консилиума при загрузке НЕ отбрасывается (отступ от Р3
  дизайна) — остаётся с предупреждением, проверяется при созыве (шаг 6). Причина: `available()`
  зависит от каталога запуска, и `/profile save` стёр бы имя с диска.
- Шаг 4, просмотр: стадия, у которой были переходы, но ни один не годен, делает негодной всю
  карту — иначе она молча становилась конечной.
- Имя стадии приводится одной функцией `machine.имя_стадии`, её зовёт и `workspace.set_stage`.
- `machine` не импортирует на верхнем уровне `profiles`, `state`, `team`, `agent`, `output`.
- Шаг 2, просмотр: вес памяти всегда считается по ПОЛНОМУ раскрытию (с звеньями и
  рассуждениями), а в запрос пара уходит по факту инструментов. Признак «как собран прошлый
  запрос» отвергнут: он устаревает между сборками, и первый запрос с инструментами после
  запуска или снятия паузы выбрасывал бы пары дословно. Перевес при паузе — безопасная сторона;
  у профилей без карты приложений нет, и вес не меняется.
- Шаг 3, просмотр: круг сверяет поколение памяти перед каждым вызовом и потоком (`/clear`
  останавливает круг), не проглатывает отмену из исполнителя; перед каждым следующим кругом
  оценивает окно и при превышении ОТПРАВЛЯЕТ запрос с предупреждением (отказ сами отвергнут:
  противоречит требованию tokens «отказывать самим нельзя», отвергнутый сервером запрос не
  оплачивается); вес описаний инструментов агент запоминает с прошлого обмена для полоски и
  предупреждения (отставание на один обмен принято).
- Шаг 3, открыто для живого прогона: служебная обёртка сервера вокруг `tools` не мерена — снять
  числом (`prompt_tokens` с инструментами минус без них минус `count_tools`).
- Живой прогон мастера (замороженная копия 7b08753, flash): на описании сценариста три вопроса
  (стиль, формат, ограничения), рассуждения выключены, температура 1.3; стадии легли текстом
  в инструкцию (ожидаемо до шага 9); консилиум лёг в `agents` — находка, внесена в шаг 9.
  Для видео профили консилиума (режиссёр, продюсер, знаток-романа) заводятся ДО сценариста.
- 2026-09-16, по просьбе пользователя до видео: профили консилиума «режиссёр», «продюсер»,
  «знаток-романа» заведены мастером (замороженная копия 7b08753) в `~/.config/myharness/profiles`.
  У «продюсера» мастер поставил `agents: [режиссёр]` — поле убрано руками; случай внесён в шаг 9.
- Шаг 5: `machine.набор(state, profile)` — профиль передаётся явно (договор говорил
  `набор(state)`): поставщик у главного агента связан с его профилем, а не с тем, что лежит в
  состоянии в миг вызова. `workspace.обновить` шире договора (`дописать`, `очистить`) — ради
  одной записи на переход.
- Шаг 5, отступ от Р7 дизайна: строка «Есть «Сейчас» — продолжай с него…» стоит в хвосте, а не
  в неизменной части: она появляется только при непустом «Сейчас».
- Шаг 5, просмотр: ворота отказывают по `ждёт: человек`; `/task этап` снимает ключи ворот;
  итог стадии на утверждении — ключ заголовка `итог` (его переложит в «Сделано» `/task
  утвердить` шага 7); незакрытые шаги плана называются, при переходе уходят в «Находки»,
  перехода не запрещают.
- Шаг 6: `team.созвать(state, вопрос, имена, *, владелец)`; `run` зовёт не `созвать`, а общие
  части `_загрузить` и `_опросить` (у группы и консилиума различаются экран, тексты, сводка).
  Доска консилиума — экран `<владелец>:council` без сводки.
- Шаг 6, просмотр: ответы участников идут рамками как данные; `/clear` посреди созыва отменяет
  запросы участников (ожидание исполнения вместе с сигналом очистки); сбой участника — «не
  ответил», остальные не сиротеют.
- Живой прогон консилиума — после шага 8, в настоящем окне.
- Шаг 7, просмотр: `/task этап X` у задачи с картой идёт через `применить_переход` (отступ от
  плана «set_stage»): иначе итог стадии пропадал молча и появлялся второй путь перехода. У
  задачи чужого профиля `/task этап` отказывает с подсказкой открыть профиль.
- Справка `/task` и подсказка `/task этап` по стадиям карты — к шагу 8 (владеет `ui.py`).
- Шаг 9: живой прогон мастера на описании сценариста — карта из шести стадий, approval у RESEARCH и PLAN, council у RESEARCH, agents пуст.
- Шаг 8, живой прогон через cli.repl (flash): при температуре 1.3 без рассуждений (так мастер
  дня 12 настроил «сценариста» и «режиссёра») модель на длинном входе с инструментами уходит в
  бессвязный текст, упирается в max_tokens, move_stage не зовёт; вопрос консилиуму испорчен.
  С включёнными рассуждениями тот же прогон прошёл целиком: консилиум → move_stage → «ждёт
  вас» в строке состояния → /task утвердить → PLAN. Открытый вопрос пользователю: что делать с
  температурой, которую называет мастер.
- Шаг 8: результат инструмента печатает отрисовка по событию агента `tool_result` (отступ от
  плана «печатает исполнитель»).
- 2026-09-16, пользователь: в конвейер разработки (навыки `/задание`…) добавить стадию «проверка
  спецификаций на противоречивость требований». Решение ведущего: отдельная общая задача в
  `main` сразу после закрытия дня 13 (сегодня противоречия commands ↔ task-machine дважды нашёл
  только независимый просмотр; `openspec validate` смысла не проверяет).
- 2026-09-16, пользователь: мастер у профиля с картой держит рассуждения включёнными (программно,
  вслух); профили режиссёр и знаток-романа переводятся на рассуждения до видео; подбор
  температуры мастером — отложенная задача 12.
- Шаг 8, просмотр: токены рассуждений круга берутся из его `meta`; строка результата несёт имя
  инструмента; строка состояния у задачи чужого профиля называет профиль.
- Видео запускается с MYHARNESS_PROFILES=~/.config/myharness/profiles: иначе поиск вверх из test/ находит profiles/ в корне репозитория, и мастер пишет туда.
