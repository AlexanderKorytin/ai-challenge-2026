# Отложенные задачи — подробности

Вынесено из `CLAUDE.md` 2026-09-12: подробные описания не нужны в каждом запросе.
Краткий список — в `CLAUDE.md`, раздел «Отложенные задачи».

| # | Задача | Откуда взялась | Объём |
|---|---|---|---|
| 1 | **Своё выделение мышью в harness.** Постановка переписана 2026-09-16 по живым замерам: прежняя опиралась на две неверные посылки. Первая — будто мешает отслеживание протяжки: его инструмент не просит с 2026-09-07 (фиксация `3b7837a`), просит только `1000h` и `1006h`, и это видно в снимке псевдотерминала. Вторая — будто Claude Code перехватывает протяжку и выделяет сам: тот же снимок с `claude` показывает `2004h`, `1004h`, `2031h` и НИ ОДНОГО режима мыши — в замеренном запуске образец мыши не просил и в дополнительный экран не уходил, оттого выделение у него и работает (строки `1000h`, `1006h` и `1049h` в исполняемом файле есть, значит в каких-то режимах он их поднимает; в каких — не выяснено). Настоящая причина беды — сам поднятый `1000h`: пока нажатия запрошены, штатный Terminal.app отдаёт приложению левую кнопку. Обход есть и проверен пользователем — тянуть с зажатым **Fn** (`⌥` в Terminal.app не помогает; какая клавиша в других терминалах — решает терминал, живьём проверен только Terminal.app), и 2026-09-16 решено закрыть беду справкой, а саму задачу отложить. Что остаётся сделать, когда Fn окажется мало: просить `1002h`, помнить нажатие-протяжку-отпускание, брать знаки из отрисованного экрана `prompt_toolkit` (`Screen.data_buffer`, `renderer._last_screen`), подсвечивать выделение, двойной щелчок — слово, тройной — строка, копировать через слой буфера обмена. Слой строить по образцу: `pbcopy` на macOS, `wl-copy`/`xclip` на Linux, `OSC 52` при заходе по `ssh`, с обёртками `tmux` (`raw+dcs`) и `screen` (нарезка DCS) — у Claude Code он устроен ровно так. Главная цена названа честно: соответствия «экранная строка → исходный текст» в проекте нет вовсе, лента живёт плоским списком пар «стиль, текст» (`screens.py:69`), связку придётся строить через `render_info` и `UIContent` и дальше поддерживать вечно — переносы, широкие знаки, прокрутка за край. | День 8 (2026-09-09): не удалось скопировать текст из окна ответов. Переставлена 2026-09-16 после замеров и разбора подходов — дизайн `docs/выделение-мышью.md` | день: свой штурм, слой буфера обмена, разбор протяжки, подсветка, требование в `openspec/specs/interface/`, проверки |
| 2 | **Усечение большой вставки у источника.** Claude Code режет не накопленную историю, а **отдельную вставку**, ещё до того как она попадёт в контекст: чтение файла ограничено числом строк, длинный вывод инструмента усекается. У `myharness` инструментов пока нет, и единственный источник текста — сам человек; молча укорачивать его вопрос нельзя, а предупреждение о переполнении окна до отправки уже есть с дня 8. Значит механизм заводится вместе с инструментами чтения файлов и вывода команд — и усекать обязан их вывод, а не разговор. | День 9 (2026-09-10): при снятии аварийного потолка по знакам выяснилось, что защита от гигантской вставки стоит не в том месте | несколько часов, но только после появления инструментов у агентов (см. задачу 3) |
| 3 | **Конвейер заданий внутри `myharness`** — планировщик, постановщики, утверждение плана человеком, исполнители волнами. Дизайн утверждён, план из 22 шагов написан, кода нет ни строчки: `openspec/changes/конвейер/`. Перед возвратом к работе план обязан быть сверен с текущим кодом — с 2026-09-08 инструмент уехал вперёд. За границами этого предложения отложены: инструменты у агентов, цикл до результата вместо одного обмена, проверка компилятором, состояние задачи дольше одного вопроса, разрешения на необратимое, изоляция контекста подзадач. **Дополнено 2026-09-17 (день 14): программная проверка инвариантов по коду проекта.** Инвариант вида «DI — только Koin, не Dagger» надёжно держит только сборка проекта: запрет зависимости в Gradle, архитектурная проверка Konsist/ArchUnit, для Python — `import-linter`. Модель может набросать такую проверку при записи инварианта, но на трёх условиях: проверку утверждает человек (иначе модель пишет сторожа себе сама); файл проверки лежит в репозитории проекта, а в раздел «Ограничения» идёт строка с командой и отпечатком файла — изменившийся файл делает проверку недействительной до нового утверждения; запускает её программа в тот момент, когда агент изменил код, как перехватчики Claude Code. Без агента, меняющего файлы, проверка смысла не имеет — потому здесь. | Решение пользователя 2026-09-08; дополнение — 2026-09-17 | несколько дней |
| 4 | **Журнал прогонов лежит не там.** `myharness-journal.jsonl` пишется внутрь дерева git рядом с песочницей и содержит все разговоры; `.gitignore` его закрывает, но место структурно неверное. Правка не сводится к смене пути: страница сравнения дня 2 читает журнал именно там. | День 7 (2026-09-08), названо в `week_2/day_2/README.md` | полдня: свой штурм, требование в `openspec/specs/journal/`, правка страницы сравнения |
| 5 | **Архивариус плодит почти одинаковые факты.** В дне 9 один прерванный прогон набил сорок фактов — то есть весь потолок, — и добрая половина повторяла соседей другими словами: «в проекте суммы ведутся в тенге, целыми», «в проекте суммы в тенге должны быть целыми, без дробной части», «суммы в его проекте ведутся в тенге целыми числами». Отбор повторов сейчас сравнивает нормализованный текст на совпадение, а такие пары не совпадают ни одним знаком. Следствия два, и оба скверные: потолок в сорок фактов упирается за один разговор, а всё накопленное уезжает в системную инструкцию КАЖДОГО запроса, то есть человек платит за пересказ одного и того же по три раза. **Подход, предложенный пользователем и уточнённый:** сверять не отдельным запросом, а в том же заходе архивариуса — дать ему на вход нынешний список фактов пронумерованным и просить два списка, что добавить и номера того, что заменить. Лишних запросов не появляется вовсе, а замена по номеру умеет то, чего не умеет простая сверка «есть или нет»: поставить точную формулировку вместо слабой. Устройство то же, что у выжимки в дне 9, и там оно проверено живьём. Цена: запрос архивариуса тяжелеет на весь список фактов — против нынешнего, где те же строки едут в системной инструкции КАЖДОГО запроса, это мелочь. | День 9 (2026-09-10): при очистке памяти перед записью видео | полдня: свой штурм, правка инструкции архивариуса, требование в `openspec/specs/memory/` |
| 6 | **Переход `myharness` с DeepSeek на OpenAI.** Это отдельная общая задача, а не часть ближайшего задания дня и не продолжение отложенного конвейера. **Сейчас ничего не переключаем:** DeepSeek остаётся единственным действующим поставщиком, его ключ, модели, профили и накопленные данные сохраняются как есть. Когда задача начнётся, нужен чистый переход на OpenAI через Responses API, без постоянной поддержки двух поставщиков: многопоставочный слой заводить только по отдельному требованию. Подробный план — `docs/отложенное/переход-на-openai.md`. | Решение пользователя 2026-09-11 после обзора проекта и вариантов перехода | несколько дней: свой штурм, новое изменение OpenSpec, переработка обмена, параметров, токенов, стоимости, настроек и живые прогоны |
| 7 | **Согласовать порог подготовки выжимки `0.5` и отметку `0.8`.** Спецификация интерфейса ставит на полосе отметку `0.8`, а `compact.py` после живого замера запускает фоновую подготовку на половине ближайшего ограничителя. Нужно отдельно решить, описывает ли `0.8` порог самой обрезки, а `0.5` — ранний запуск заготовки, либо требования и показ действительно разошлись; затем привести OpenSpec, подписи интерфейса, код и проверки к одному договору. В день 10 это не входит: у сравниваемых стратегий выжимка выключена. | День 10 (2026-09-11): при исследовании стратегий контекста найдено расхождение OpenSpec и кода; пользователь решил разобрать его отдельно завтра | точечная общая задача: свой штурм, сверка живого замера, требования `agent`/`interface`, код и проверки |
| 8 | **Branching разделяет разговор один раз, а не деревом.** В графе ровно одно поле контрольной точки (`memory.py:536`) и ровно пара имён ветвей (`memory.py:727`); узел, чья ветвь не входит в объявленную пару, при подъёме считается повреждённым (`memory.py:739`). Поэтому `/branch split` внутри ветви A упирается в ту же проверку `checkpoint_id is not None` (`memory.py:893`) — она смотрит на весь граф, а не на текущую ветвь, — и продолжить исследование ветвлением внутри ветви нельзя. Требование сформулировано так же, в единственном числе: «Граф Branching SHALL содержать **событие разделения**…» (`openspec/specs/memory/spec.md:658`). Обход есть, но слабый: другой Branching-профиль даёт свой файл со своей единственной развилкой — соседний разговор без общей истории. Настоящее дерево — это разделение от ЛЮБОГО узла и произвольное число ветвей: переделка модели графа, подъёма с проверкой связей, показа вкладок и панели выбора ветви. | День 13 (2026-09-13): вопрос пользователя по ходу разделения `cli.py`, ограничение найдено чтением кода и в тот же день **проверено пользователем в живом инструменте** — второе разделение внутри ветви не проходит. То есть это не теоретическая находка: за задачей стоит попытка сделать так, как естественно ожидалось. Попутно видно, что отказ немногословен — «разговор уже разделён», без слов о том, что развилка на разговор одна и что делать дальше; подпись стоит поправить вместе с самой задачей | сопоставимо с днём стратегий контекста: свой штурм, переработка требования `memory`, модель графа, подъём, интерфейс ветвей и проверки |
| 9 | **СДЕЛАНО 2026-09-15** (фиксации `23670f4`..`f3c61b2`; дизайн `docs/естественная-запись.md`). **Естественная запись в слои памяти.** Сейчас человек говорит инструменту, куда класть, командой: `/remember`, `/task план`, `/project Стек`. В разговоре с ведущим он говорит иначе — «запиши в задачи проекта: …», «вот тебе факт обо мне: …» — и слой выбирается сам. Согласованное устройство: фраза-зачин («запиши», «запомни», «добавь в», «отметь») запускает ОДИН дешёвый запрос, модель возвращает объектом слой, раздел и текст, программа показывает, куда собирается положить, кладёт и называет способ отменить. Явный выбор из условия задания при этом сохраняется: решение видно и обратимо, а не сделано молча. Зачин обязателен — гонять распознавание на каждой реплике значило бы удвоить расход на ровном месте. **Трогать разрешено всё, кроме раздела «Ограничения» карточки** (решение пользователя 2026-09-14): по этим строкам программа начнёт судить ответы модели, и правило, которое модель написала себе сама, проверкой не будет. | День 11 (2026-09-14): вопрос пользователя по ходу исполнения — «или как при общении с тобой: я говорю „запиши в задачи проекта“, и ты понимаешь, что и куда писать, — можно в myharness так же?»; там же решено делать отдельным днём | день: свой штурм, требование в `openspec/specs/memory/`, распознаватель, показ и отмена, проверки подставным клиентом и живой прогон |
| 10 | **Крючок и индекс у записей о человеке.** Сейчас все записи о человеке уходят в системную часть КАЖДОГО запроса целиком — сорок строк, из которых к текущему разговору относятся одна-две. Память Claude Code (`~/.claude/projects/<проект>/memory/`) устроена иначе, и это устройство видно на диске: у каждой записи есть заголовок с `description` — однострочный крючок, по которому решается, уместна ли запись сейчас, — а рядом лежит индекс `MEMORY.md`, по строке на запись со ссылкой на файл. В контекст сессии грузится ТОЛЬКО индекс; тело записи поднимается, когда крючок совпал с делом. Нам нужно то же: крючок в заголовке файла факта, индекс рядом, и подъём тела по надобности вместо подъёма всего. Смежное: у образца есть правило «перед записью проверь, нет ли записи, которая это уже покрывает, — обнови её, а не плоди вторую» — это ровно отложенная задача №5, и делать их стоит вместе: крючок даёт по чему сравнивать. | 2026-09-15, работа «естественная запись в слои памяти»: вопрос пользователя «как это реализовано в Claude Code?» — сверили устройство и увидели две недостающие части | несколько дней: свой штурм, требование в `openspec/specs/memory/`, формат заголовка факта и перенос уже записанных, индекс, правило подъёма, замер расхода до и после |
| 11 | **Формулы показываются сырым TeX.** Профиль «математика» на вопрос «что такое бесконечность?» ответил ровно так, как заказано, — определением и формулами, — но записал их разметкой TeX: `\(X\)`, `\[f:X\to\{1,\dots,n\}\]`, `2^{\aleph_0}`. Терминал показывает это как есть, и самое содержательное место ответа читается хуже всего. Дешёвое лечение — строка в инструкции занятия («формулы пишешь знаками Юникода: ℵ₀, ℝ, →, ∞»), но оно лечит один профиль и держится на том, что модель послушается; сырой TeX придёт из любого другого занятия, где уместна формула. Настоящее лечение — показ: переводить при отрисовке простые конструкции TeX в Юникод (степени и индексы, буквы греческого алфавита, ℵ, ∈, ≤, →, дроби вида a/b) и убирать обрамление `\(…\)` и `\[…\]`, а непереводимое оставлять как есть, не ломая. Границу «что переводим» надо назвать явно: полный разбор TeX в терминале — не наша задача. Ориентир прямого образца не даёт: Claude Code рисует разметку Markdown, а формул не рисует тоже — значит решение своё, и тем важнее держать его узким. | 2026-09-15, съёмка отчёта дня 12: ответ математика в кадре пошёл разметкой; слова пользователя «ты видел ответ математика — это что-то» | полдня: свой штурм, требование в `openspec/specs/interface/`, перевод при отрисовке, проверки на знаках, которые переводить нельзя |
| 12 | **Мастер профиля называет температуру, на которой модель ломается.** Мастер дня 12 сам выбирает для «творческого» занятия выключенные рассуждения и температуру 1.3. Живой прогон дня 13 (2026-09-16, `deepseek-v4-flash`) показал цену: на длинном входе такой профиль выдаёт бессвязный текст («полная чепу…»), упирается в `max_tokens`, и даже короткий ответ участника консилиума («режиссёр») рассыпался в обрывки слов. С включёнными рассуждениями тот же сценарий проходит целиком. Для профилей с картой стадий это уже закрыто программно (рассуждения включены всегда — без них не держатся вызовы инструментов); для прочих профилей подбор температуры мастером остаётся как был. Что решить: замерить качество на нескольких температурах и длинах входа у `flash` и `pro`, сверить с рекомендациями DeepSeek (там 1.3 — «беседа», 1.5 — «творческое письмо») и либо сузить выбор мастера по замеру, либо оставить рассуждения всегда и отдать температуру человеку через `/params`. Предел числом без замера не ставить. | День 13 (2026-09-16): живой прогон автомата задачи через настоящее приложение | полдня: свой штурм, замер на нескольких температурах, правка инструкции сборщика, требование `commands` «/profile new» |
| 13 | **СДЕЛАНО 2026-09-17** (фиксации `202de0a`..`fb59020`; дизайн `docs/справка-разделами.md`). **Справка разделами — меню `/help`.** Справка выросла примерно до двухсот строк, нужный абзац в ней не найти. Решение пользователя 2026-09-17 — подход А: `/help` открывает меню разделов тем же устройством, что `/model`, `/profile`, `/strategy` (стрелки, Enter или щелчок печатает раздел в ленту, Esc закрывает); `/help <раздел>` открывает раздел сразу, имена разделов подставляются при наборе. Разделы: Команды, Память, Инварианты, Автомат задачи, Карточка и естественная запись, Профиль занятия, Группа и способы, Сжатие, Токены и деньги, Мышь и клавиши. Отвергнуто: сворачиваемые разделы прямо в ленте (подход Б) — такой механики в ленте нет, её пришлось бы заводить с щелчками по строкам и состоянием каждого заголовка. Ориентир — Claude Code: там `/help` — отдельная панель с переключением стрелками. Проверки справки (`check_app`, `check_layers`) ищут текст в `ui.help_fragments()` — их придётся перевести на разделы. | 2026-09-17, после отчёта дня 14: «можно разделить help на раскрывающиеся разделы»; делать отдельной сессией | несколько часов: свой штурм, требование `commands` «справка», разбиение текста, меню, проверки |
