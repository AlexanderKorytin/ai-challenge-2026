# Состояние: подключение MCP (день 16)

**Род работы:** задание дня 16 «Подключение MCP». Код — в `main`; ветка `w4d1` и папка
`week_4/day_1/` заводятся после кода, под постановку, песочницу и видеоотчёт (если пользователь
подтвердит папку).
**Ведущая среда:** Claude Code.

## Стадия Research

- **2026-09-21.** Профильный разбор не брался: фактические вопросы (версия библиотеки, способы
  передачи, какие публичные серверы живы без ключа) закрыты живой пробой в песочнице ведущего —
  дешевле и достовернее мнения помощника. Итог пробы — в `docs/подключение-mcp.md`.
- В рабочем дереве `main` лежат незафиксированные правки дня 15 («прыжок мимо карты только
  `силой`», стадия `ОТКЛОНЕНА`): `commands_task.py`, `check_layers.py`, две спецификации,
  `docs/состояние-контроль-стадии.md`. К дню 16 не относятся, в его фиксации не включаются.

## Открытые вопросы

- Нет.

## Решения пользователя

- **2026-09-21.** Дизайн утверждён. Папку `week_4/day_1/` заводить. Правки дня 15 — зафиксировать
  отдельно. Добавлено: команды добавления и удаления серверов — вписаны в дизайн разделом
  «`/mcp add` и `/mcp remove`» (ведущий дополнил синтаксис по образцу Claude Code и правило о
  буквальном `Authorization`; показано пользователю в сводке плана).

## Стадия Plan

- **2026-09-21.** План — `docs/план-подключение-mcp.md`, три шага; показан пользователю, ждёт
  согласия. Правки дня 15 зафиксированы отдельно: `a7f2b35` (просмотр нашёл противоречие в
  `commands/spec.md`, исправлено до фиксации).
- **2026-09-21.** План согласован пользователем (вместе с синтаксисом `add`/`remove`, правилом
  имени и правилом о буквальном `Authorization`).

## Стадия Spec

- **2026-09-21, первый рубеж.** Проверял general-purpose (opus), один заход. Найдено четыре:
  справка закреплена десятью разделами (→ одиннадцатый раздел `mcp` в «Состав команд»);
  видимость `/mcp` до авторизации не решена (→ видна, ключа не требует); Purpose `config`
  «единственный секрет» станет ложным (→ уточнение в шаге 3); `mcp__…` названо именем функции,
  хотя часть от инструмента не проверяется (→ в этот день только показ, правило имени функции —
  следующий день). Все закрыты правкой раздела плана; решений не меняют — повторный заход не
  брался. Мелочь о подстановке имён после `remove` — не подставляются, записано. Вердикт: чист.

## Стадия Executing

- **2026-09-21.** Основа: `0b3f070`.
- **Шаг 1, просмотр** (general-purpose, opus): беды — зависший stdio-сервер вешает `/mcp`
  навсегда (у библиотеки для stdio предела нет); одна запись SSE ломает весь файл; проверка
  вычистки тайн пустая. Приняты также: код ответа HTTP в сбое (401 был невидим), права черновика
  с момента создания, вычистка значений каждой переменной во всех полях. Мелочи отвергнуты:
  символьная ссылка `.mcp.json`, `exceptions[0]` группы.
- **Решение пользователя 2026-09-21:** предел запуска сервера — как у Claude Code: 30 с,
  переопределение `MCP_TIMEOUT` (миллисекунды). Зависший сервер — строка сбоя, прочие показаны.
- **Шаг 1 закрыт.** Повторный просмотр (тот же проверяющий, расширен на весь шаг — договор
  изменился) — бед нет. Мелочь «код ответа DELETE 405 приписывается чужой причине» исправлена
  ведущим (только POST). Отвергнуто: символьная ссылка `.mcp.json`, дробный `MCP_TIMEOUT`,
  истечение предела во время уборки после полного списка. Открыто на шаг 2: библиотека пишет
  предупреждения через `logging` — поверх экрана `prompt_toolkit`, если обработчики не настроены.
- **Шаг 2 закрыт.** Просмотр (general-purpose, opus), один заход: беды — сдвиг ленты при замене
  строки ожидания итогом другой длины (идущий обмен затирал бы чужое) и повтор довода с токеном в
  отказе по формату заголовка. Исправлено ведущим приёмом `_снять_ожидание` и отказом без довода;
  попутно закрыта мелочь «токен с подстановкой в хвосте обходил отказ». Повторный заход не брался:
  исправления — известный приём проекта и две строки отказа, проверены парными проверками и живым
  прогоном. Попутная правка: `visible_commands` до авторизации показывает команды без ключа, как
  требует `commands` («Меню показывает только применимые команды») — код с требованием расходился.
  Отвергнуто: предупреждение о токене в адресе, регистр имён заголовков, перенос `NullHandler` в
  `mcp_client`.
