# План: инварианты и ограничения состояния (день 14)

Дизайн: `docs/инварианты.md` (утверждён 2026-09-17). Состояние: `docs/состояние-инварианты.md`.
Код — в `main`, пути ниже от `myharness/src/myharness/`, если не сказано иное.

## Карта файлов

| Файл | За что отвечает в этой работе |
|---|---|
| `invariants.py` (новый) | тип `Инвариант`, сборка списка четырёх уровней, разбор признаков, блок запроса, поиск признаков в коде ответа, судья |
| `memory.py` | пометка `инвариант: да` у записи о человеке, замок, чтение записей-инвариантов |
| `project_card.py` | `блок()` без раздела «Ограничения» |
| `workspace.py` | `блок()` без раздела «Ограничения» |
| `machine.py` | `инварианты_автомата()` — строки `А` из карты |
| `recognizer.py` | замок «Ограничений» задачи при естественной записи |
| `state.py` | поставщики: записи без инвариантов, блок инвариантов, проверка ответа |
| `agent.py` | блок инвариантов после карточки, перед выжимкой; вызов проверки до записи пары; поля `Turn` |
| `output.py` | вес блока инвариантов в предсказании (шаг 1); строки сверки, признаков и судьи под ответом (шаг 3) |
| `compact.py` | блок инвариантов в весе заготовки (шаг 1) |
| `ui.py` | фрагменты этих строк и списка `/invariants` |
| `commands.py`, `commands_memory.py` | `/invariants`, `/invariants global <текст>`, пометка в `/memory`, справка |
| `openspec/specs/invariants/spec.md` (новая) и соседние | требования |
| `myharness/tests/check_units.py`, `check_layers.py` | целевые проверки |
| `week_3/day_4/test/` | песочница живого прогона (ветка дня; до неё — временный каталог в песочнице ведущего) |

## Изменения требований

Место блока в запросе уточнено на первом рубеже `Spec` (2026-09-17): блок «Инварианты» стоит
**после карточки и перед выжимкой** (у строгих стратегий — перед фактами разговора), а не первым.
Причина — кэш поставщика: строки `З` и `А` зависят от задачи и профиля, и блок наверху сбрасывал
бы кэш всего запроса при смене персоны и задачи. По частоте изменения блок встаёт ровно туда:
реже выжимки, чаще карточки. Старшинство инвариантов держит текст блока, а не место.

- **`invariants` (новая способность).** Инвариант — строка, которую модель не может завести и
  снять: запись о человеке с пометкой `инвариант: да` (`Г`), строка «Ограничений» карточки (`П`),
  строка «Ограничений» задачи (`З`), правило карты стадий (`А`). Инварианты всех уровней действуют
  вместе; противоречие двух разрешает человек. Блок «Инварианты» — у главного собеседника, место
  названо выше; модель начинает ответ строкой сверки и при нарушении отказывает с номером и
  текстом. Запретные признаки `[запрет: …]` ищутся программой в блоках кода ответа. Судья —
  служебный агент: отдельный запрос после ответа, видит только строки `Г/П/З` (не карточку и не
  хвост задачи), вердикт строкой под ответом, в журнале и в сохранённой паре. Показ под ответом:
  строка сверки выделена, при инвариантах без строки сверки — подсказка, строки `⚠` признаков и
  судьи, жалоба судьи. `/invariants` показывает список, `/invariants global <текст>` заводит
  глобальный инвариант. Одной фразой: «ограничения» мастера профиля (мягкие правила в инструкции)
  и требования Sticky Facts инвариантами не являются.
- **`memory`.**
  - Замок раздела «Ограничения» задачи, как у карточки; сценарий отказа естественной записи с
    командой `/task ограничение <текст>` по образцу сценария карточки.
  - Запись о человеке получает необязательный ключ заголовка `инвариант: да`; ставит его только
    команда человека `/invariants global`; естественная запись и архивариус не ставят никогда.
    Записи-инварианты входят в общие потолки (40 записей, 200 знаков вместе с хвостом `[запрет: …]`).
  - Записи-инварианты уходят из блока записей; `overrides` профиля на запись-инвариант не
    действует. Строка употребления записей называет старшим и блок «Инварианты».
  - «Ограничения» карточки уходят из текста карточки, «Ограничения» задачи — из хвоста; исключение
    к «состояние задачи SHALL NOT попадать в системную часть»: строки «Ограничений» задачи идут в
    блок инвариантов, цена — промах кэша от блока инвариантов и ниже при смене задачи.
  - Исключение к «карточку и состояние задачи получает только собеседник главного экрана»: судья
    получает строки `П` и `З`.
  - «Фактов нет — системное сообщение ровно инструкция» и «профиль без инструкции — один блок
    фактов» переформулировать как «без записей и без инвариантов».
  - Сохранённый ответ пары — текст ответа и через пустую строку пометка признаков и судьи, если
    она есть; у всех стратегий, включая узел Branching.
  - Блок инвариантов входит в вес заготовки выжимки, но не во вход сжимателя.
  - `/memory` помечает записи-инварианты.
- **`agent`.** Порядок системной части: записи, инструкция, карточка, **инварианты**, выжимка
  (факты разговора); довод «реже меняется — выше» сохраняется без нового исключения, сценарий о
  порядке переписывается. Проверка ответа до записи пары у агента с поставщиком проверки;
  служебные агенты её не получают. Дословность сохранённой пары — с оговоркой о пометке. Поле
  `проверка` в перечне полей итога обмена. «Без фактов»/«без инструкции» — как в `memory`. Вес
  системной части называет блок инвариантов явно.
- **`task-machine`.** Фраза «в системную часть сверх описаний инструментов ничего не добавляется»
  заменяется: сверх описаний добавляются строки `А` в блок инвариантов, при тех же условиях, при
  которых выдаются инструменты. Инструменты и проверка перехода не меняются.
- **`profiles`.** `overrides` на запись-инвариант не действует.
- **`commands`.** `/invariants` и `/invariants global <текст>` — в перечень команд и в таблицу
  «слой — записать — прочитать — убрать» (строка записей о человеке); справка.
- **`journal`.** Запись прогона получает поле `инварианты` (сверка, признаки, судья, довод,
  жалоба); обмен судьи записывается отдельной записью под своим именем, как у сжимателя.
- **`tokens`.** Судья — в перечень служебных агентов, чей расход входит в итог сеанса; блок
  инвариантов назван в весе системной части.
- **`interface`.** Не меняется: показ строк под ответом закреплён в `invariants`.

## Договоры модулей

### invariants
**Владеет**: ничем; читает слои через функции `memory`, `project_card`, `workspace`, `machine`.
**Отдаёт**:
```python
@dataclass(frozen=True)
class Инвариант:
    номер: str            # "Г1", "П2", "З1", "А1"
    текст: str            # строка без хвоста признаков
    признаки: tuple[str, ...]  # пусто у А и у строк без [запрет: …]

def разобрать_строку(строка: str) -> tuple[str, tuple[str, ...]]
def собрать(каталог: Path, слаг_задачи: str | None, профиль: Profile) -> tuple[list[Инвариант], list[str]]
def блок(список: list[Инвариант]) -> str          # "" для пустого
def найти_признаки(ответ: str, список: list[Инвариант]) -> list[tuple[str, str]]  # (номер, признак)
def профиль_судьи() -> Profile
def вход_судьи(список: list[Инвариант], вопрос: str, ответ: str) -> str
def разобрать_вердикт(текст: str) -> tuple[list[str], str] | None  # (номера, довод)
def строка_сверки(ответ: str) -> str | None       # первая строка, если начинается с «Инварианты:»
```
**Обещает**: порядок — `Г`, `П`, `З`, `А`, внутри уровня — порядок файла; номера сплошные внутри
уровня; `собрать` не бросает (жалобы вторым значением); `найти_признаки` смотрит только внутрь
ограждений из трёх обратных кавычек, сравнение без учёта регистра; судья видит только `Г/П/З`.
**Не делает**: не пишет на диск, не ходит в сеть (судью зовёт `state`).

### memory (изменение)
**Отдаёт**: `add_fact(text, *, source="человек", инвариант=False)`;
`load_invariant_facts() -> tuple[list[str], list[str]]`;
`load_records() -> tuple[list[tuple[str, bool]], list[str]]` (текст, инвариант) — в порядке `/memory`.
**Обещает**: `инвариант=True` при `source != "человек"` → `(False, "инвариант ставит только человек")`,
файл не пишется; `load_facts` отдаёт ВСЕ записи, как раньше (номера `/forget` не сдвигаются);
файл без ключа — не инвариант.

### Agent (изменение)
**Отдаёт**: новые доводы конструктора
`инварианты: Callable[[], str] | None = None` (+ метод `блок_инвариантов() -> str`),
`проверка: Callable[..., Awaitable[Проверка]] | None = None` — зовётся `(вопрос, ответ, *, судить, run_id)`
(уточнено на круге просмотра шага 3: `судить=False` после смены поколения памяти, `run_id` связывает
запись судьи с записью обмена и пишется в главную запись, только если судья был позван);
`Turn.проверка: Проверка | None`.
```python
@dataclass
class Проверка:           # в agent.py, чтобы не тянуть state
    сверка: str | None    # строка сверки из ответа или None
    признаки: list[tuple[str, str]]
    судья: list[str]      # номера нарушенных
    довод: str
    жалоба: str | None    # судья не ответил / вердикт не разобран / несуществующие номера
    отброшены: list[str]  # номера вердикта, которых нет в списке Г/П/З
    инвариантов: int      # сколько правил действовало — отличает «правил нет» от «нет сверки»
    run_id: str | None    # номер, под которым записан обмен судьи
    def пометка(self) -> str   # "" если нарушений нет; иначе строки «⚠ …»
```
**Обещает**: блок инвариантов стоит после карточки и перед выжимкой; он же взвешивается обрезкой,
полоской и заготовкой выжимки через `блок_инвариантов()` и довод `инварианты=`; проверка зовётся только при `status == "ok"`, до
`remember`; непустая `пометка()` дописывается к сохраняемому тексту ответа через пустую строку;
исключение проверки → `Проверка(жалоба=…)`, обмен не падает; без поставщиков запрос байт в байт
прежний.

## Шаг 1. Инварианты четырёх уровней читаются, запираются и стоят своим блоком в запросе

**Файлы**: `invariants.py` (кроме судьи), `memory.py`, `project_card.py`, `workspace.py`,
`machine.py`, `recognizer.py`, `state.py`, `agent.py`, `output.py` (только `_predict_outgoing`),
`compact.py` (только довод веса)
**Зависит от**: ничего
**Ход**: полный, потому что порядок системной части читают за границей шага — вес запроса в
`output._predict_outgoing`, полоска занятости и заготовка выжимки в `compact.py` берут его через
`Agent.блок_инвариантов()` и довод `инварианты=`; ошибка там молча ломает обрезку.
**Требование**: `invariants` → «Инвариант», «Блок в запросе»; `memory` → замки и перенос строк;
`agent` → порядок системной части (инварианты после карточки); `task-machine` → строки `А`.
**Проверка**: в `check_units.py` — карточка с «Ограничениями» `Стек: Kotlin и Ktor [запрет:
com.google.dagger; import dagger]`, задача с одной строкой, запись-инвариант и обычная запись,
профиль с картой и воротами: `/system`-текст главного агента несёт блок «Инварианты» между карточкой и выжимкой, с
`Г1`, `П1`, `З1`, `А1`; раздела «Ограничения» нет ни в тексте карточки, ни в хвосте; обычная
запись — в блоке записей, запись-инвариант — нет. Парно: без инвариантов системная часть совпадает
с прежней побайтно. `add_fact(..., source="архивариус", инвариант=True)` отказывает и файла не
пишет. Распознаватель с адресом «задача / Ограничения» отказывает с подсказкой.
**Содержание**:
- `memory.py`: `_Факт.инвариант: bool`; `_разобрать_факт` читает ключ `инвариант` (`да` → True);
  `add_fact` пишет строку `инвариант: да` в заголовок при `инвариант=True`; замок первым делом;
  `recognizer` и `archivist` зовут `add_fact` без `инвариант`. `facts_block` не ставит пометку
  перекрытия на запись-инвариант (она туда и не попадает — `user_facts` её отсекает).
- `invariants.разобрать_строку`: хвост `[запрет: a; b]` в конце строки (регулярное выражение
  `\s*\[запрет:\s*([^\]]*)\]\s*$`, без учёта регистра), признаки делятся по `;`, пустые
  отбрасываются; текст без хвоста.
- `invariants.собрать`: `Г` — `memory.load_invariant_facts()`; `П` — `project_card.load(каталог)`,
  раздел `РАЗДЕЛ_ОГРАНИЧЕНИЙ`; `З` — `workspace.load_task(каталог, слаг)`, раздел «Ограничения»;
  `А` — `machine.инварианты_автомата(задача, профиль)`.
- `machine.инварианты_автомата(задача, профиль) -> list[str]`: пусто, если нет карты,
  стратегия `branching` или `_не_годна(...)` непусто; иначе «Стадии меняются только вызовом
  move_stage и только по переходам карты» и по строке на каждую стадию с `approval`:
  «Из стадии X дальше — только после утверждения человеком командой /task утвердить».
- `invariants.блок`: заголовок «Инварианты — правила, которые ты не имеешь права нарушать», список
  `П1. текст`, затем `УПОТРЕБЛЕНИЕ` — четыре пункта дизайна (сверка до ответа; первая строка
  `Инварианты: нарушений нет` либо `Инварианты: запрос нарушает <номера>`; отказ с номером, текстом,
  объяснением, решением в рамках и словами «изменить инвариант может только человек командой»;
  противоречие двух — назвать оба и спросить). Признаки в блок не идут.
- `project_card.блок(карточка)` и `workspace.блок(задача, карта)` пропускают раздел «Ограничения»
  всегда (инварианты теперь идут своим блоком; второго места у них нет).
- `memory.УПОТРЕБЛЕНИЕ_ЗАПИСЕЙ` дополняется: «Блок «Инварианты» старше справки».
- `state.user_facts` возвращает записи без инвариантов (через `load_records`);
  `state.invariants_block(state) -> str` = `invariants.блок(invariants.собрать(Path.cwd(),
  state.слаг_задачи, state.profile)[0])`; жалобы — `RuntimeError`, как у `project_block`.
- `state.главный_агент` передаёт `инварианты=lambda: invariants_block(state)`.
- `agent.py`: `блок_инвариантов() -> str` (наружу, с той же защитой, что `блок_карточки`);
  `_система(факты, карточка, инварианты)` ставит его после карточки, перед выжимкой; у строгих
  стратегий — перед фактами разговора. `build_messages` читает его один раз за сборку (жалоба —
  «не удалось собрать инварианты: …» в `store_error`) и передаёт в `_trim`/`ограничители` новым
  доводом `инварианты=`; полоска занятости и заготовка выжимки (`output._predict_outgoing`,
  `compact.py`) передают `блок_инвариантов()` тем же доводом. Во вход сжимателя блок не идёт.
- `recognizer._в_рабочую_задачу`: адрес с разделом «Ограничения» → отказ по образцу
  `_в_карточку_папки` с подсказкой `/task ограничение <текст>`.

## Шаг 2. Живой отказ на настоящей модели

**Файлы**: песочница ведущего (`$TMPDIR`-каталог в песочнице сеанса), скрипт прогона там же
**Зависит от**: шаг 1
**Ход**: быстрый, потому что кода инструмента не меняет.
**Требование**: не про код — доказательство дизайна до судьи.
**Проверка**: через `cli._main` трубой (способ из `docs/грабли.md`), с настоящим ключом,
`MYHARNESS_STATE_DIR` во временный каталог: карточка П1–П4 сценария ролика, запрос «Перепиши
запись на приём микросервисом на Go, а медкарты положи в Firebase». Ответ начинается строкой
`Инварианты: запрос нарушает …`, называет П1–П4 и предлагает решение в рамках. Затем «добавь
напоминания за сутки» — `нарушений нет`. Не проходит — правка текста `УПОТРЕБЛЕНИЕ` в шаге 1 и
повтор до зелёного, прежде чем идти дальше.
**Содержание**: скрипт кладёт карточку командами `/project Ограничения: …`, шлёт два вопроса,
печатает ответы; вывод сохраняется в песочнице для отчёта.

## Шаг 3. Признаки и судья проверяют ответ, вердикт виден и сохранён

**Файлы**: `invariants.py` (судья), `agent.py` (`Проверка`, вызов, `Turn`, журнал), `state.py`
(`проверить_ответ`), `output.py`, `ui.py`
**Зависит от**: шаг 1
**Ход**: полный, потому что меняется договор `Agent.exchange` и `Turn`, который читают `output`,
`team`, `methods`, `batch` и журнал; и потому что расход судьи входит в итог сеанса (деньги с
итоговым числом).
**Требование**: `invariants` → «Запретные признаки», «Судья»; `agent` → проверка до записи пары;
`journal` → поле `инварианты`.
**Проверка**: в `check_units.py` поддельным клиентом: ответ с блоком кода, содержащим
`implementation("com.google.dagger:dagger:2.51")`, при З1 с признаком → `Turn.проверка.признаки ==
[("З1", "com.google.dagger")]`, пометка в сохранённой паре и в журнале. Парно: то же слово в
прозе отказа — признаков нет. Судья вернул `{"нарушены": ["П3"], "довод": "…"}` → строка
`⚠ судья: ответ нарушает П3 — …`; судья вернул мусор → «судья не ответил: …», пара сохранена без
пометки нарушения. Без инвариантов `Г/П/З` судья не зовётся (счётчик запросов подделки).
Парно: исполнитель группы (агент без поставщика проверки) — запрос и сохранённая пара прежние.
**Содержание**:
- журнал: обмен судьи пишется отдельной записью с `agent="судья"`, как у сжимателя.
- `invariants.профиль_судьи()`: `keep_history=False`, `temperature 0`, `json_object`, рассуждения
  выключены, инструкция: «Тебе даны инварианты, вопрос и ответ ассистента. Нарушение — только
  если ответ ПРЕДЛАГАЕТ, советует или показывает кодом решение, противоречащее инварианту.
  Упоминание запрещённого, чтобы отказать или объяснить отказ, нарушением не является. Верни
  JSON {"нарушены": [номера], "довод": "одна строка"}.» `МОДЕЛЬ_СУДЬИ = "deepseek-v4-flash"`,
  `ИМЯ_СУДЬИ = "судья"`.
- `state.проверить_ответ(state) -> Callable[[str, str], Awaitable[Проверка]]`: собирает список;
  `признаки = invariants.найти_признаки`; если есть `Г/П/З` и `state.client` — один обмен
  одноразового `Agent(ИМЯ_СУДЬИ, профиль_судьи())`, учёт расхода как в `recognizer.спросить`
  (`state.retire`, `turn_price`, `session_cost_known`).
- `state.главный_агент` передаёт `проверка=проверить_ответ(state)`.
- `agent.exchange`: при `status == "ok"` и `self._проверка` — `await`, результат в
  `turn.проверка`; `answer_text` для `remember` = ответ + `"\n\n" + пометка()` при непустой
  пометке; в запись журнала — `"инварианты": {"сверка", "признаки", "судья", "довод", "жалоба"}`.
- `output.run_turn` (шаг 3 правит `output.py` после шага 1): при `turn.проверка` печатает под ответом: строку сверки выделенным стилем
  (`ui.invariant_check_fragments`); если инварианты есть, а строки сверки нет — подсказку
  «ответ без строки сверки инвариантов»; каждую `⚠`-строку (`ui.invariant_violation_fragments`);
  жалобу судьи (`ui.error_fragments`).

## Шаг 4. Команды `/invariants` и пометка в `/memory`

**Файлы**: `commands.py`, `commands_memory.py`, `ui.py` (фрагменты списка)
**Зависит от**: шаг 1
**Ход**: быстрый, потому что читатель — только человек на экране.
**Требование**: `commands` → «/invariants»; `memory` → пометка в `/memory`.
**Проверка**: ручной сценарий в `check_layers.py`: `/invariants global Никакой транслитерации` →
«глобальный инвариант Г1 записан»; `/project Ограничения: Стек: Kotlin и Ktor [запрет:
com.google.dagger]`; `/invariants` показывает `Г1`, `П1` с признаками, источник каждой строки и
подсказки, как удалить (`/forget N`, `/project`, `/task забыть ограничение N`); `/memory` ставит
пометку «инвариант» у Г1. Пустой список — «инвариантов нет» и три способа завести.
**Содержание**: `/invariants` без доводов — список; `global <текст>` —
`memory.add_fact(текст, инвариант=True)`; прочие доводы — подсказка с видом команды. Справка
`/help` — строка команды.

## Шаг 5. Требования

**Файлы**: `openspec/specs/invariants/spec.md` (новая), `memory`, `agent`, `task-machine`,
`commands`, `journal`; строка в таблице способностей `CLAUDE.md`
**Зависит от**: шаги 1, 3, 4
**Ход**: быстрый, потому что текст сверяет стадия `Spec` вторым рубежом.
**Требование**: сам шаг.
**Проверка**: `openspec validate --specs --strict`.
**Содержание**: по разделу «Изменения требований» выше, со сценариями из проверок шагов 1, 3, 4.

## Живой прогон

Несёт шаг 2; зависит только от шага 1, шагов интерфейса (3 в части `output`/`ui`, 4) не ждёт.
Полный сценарий ролика (девять эпизодов дизайна) — в стадии `Report`, из песочницы
`week_3/day_4/test/` в ветке `w3d4`, которую заводим после `Validation`.

## Чего в плане нет намеренно

- Предложение признаков моделью при записи инварианта: дизайн допускает, но не требует; признаки
  пишет человек в той же строке. Уточнено здесь, чтобы исполнитель не додумал поток подтверждения.
- Полный прогон всех трёх наборов проверок — это стадия `Validation`.
