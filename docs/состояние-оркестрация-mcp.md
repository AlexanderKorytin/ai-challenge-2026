Род работы: задание дня 20 (ветка `w4d5`, папка `week_4/day_5/`). Ведущая среда: Claude Code.

# Состояние: оркестрация MCP (день 20)

## Стадия

Research — 2026-09-25: профильный разбор (mcp, агенты, безопасность) проведён, дизайн `docs/оркестрация-mcp.md` написан, утверждён пользователем 2026-09-25 («да»). Plan — план `docs/план-оркестрация-mcp.md` согласован пользователем 2026-09-25 («да»).

Spec, рубеж 1 — требований не задевает (раздел плана «Изменения требований»). Вход в Executing.

## Установлено разведкой

- Маршрутизация по нескольким серверам в `myharness` уже есть: имя функции
  `mcp__<сервер>__<инструмент>`, вызов уходит своему держателю соединения (`mcp_tools.py`).
- Круг вызовов без своего предела числа кругов; вызовы видны на экране строкой `⚙ имя: …`,
  порядок лежит в журнале полем `звенья`.
- Готовые серверы: `cbr` (VPS 188.120.230.58, курсы ЦБ), `pipeline` (местный, Википедия →
  выжимка → файл), официальные `git` (`uvx mcp-server-git`) и GitHub из дня 16.

## Решения пользователя

- 2026-09-25: дизайн утверждён; `saveToFile` остаётся развилкой выбора.
- 2026-09-25: план согласован.

## Открытые вопросы

- Расхождение помощников: `mcp-teacher` — убрать `saveToFile` (пересечение с `write_file`), `ai-agents-teacher` — оставить как развилку выбора. В дизайне — оставить, роли инструкцией не называть.
