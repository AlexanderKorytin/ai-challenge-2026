# Состояние: пайплайн MCP-инструментов (день 19)

Род: задание дня 19 (ветка `w4d4`, заводится после Research). Среда: Claude Code.

## Стадия
Executing 2026-09-24. Research начат 2026-09-24. Профильный разбор не берётся: MCP-сервер, мост и круг вызовов
знакомы по дням 16–18.

## Устные решения
- 2026-09-24: поиск — Википедия (MediaWiki API, без ключа).
- 2026-09-24: всё локально на Маке, без VPS.
- 2026-09-24: выжимку делает myharness по просьбе сервера (MCP sampling).
- Дизайн: `docs/пайплайн-mcp.md`. Правка myharness нужна (sampling) — идёт в main до ветки дня.

- 2026-09-24: ДИЗАЙН УТВЕРЖДЁН пользователем («да»), вместе с папкой дня `week_4/day_4/` в ветке `w4d4`. Имена инструментов — как в условии: `search`, `summarize`, `saveToFile`.
- 2026-09-24: ПЛАН УТВЕРЖДЁН пользователем («да») вместе с решением: `maxTokens` sampling к DeepSeek не передаётся, сервер pipeline шлёт 1 048 576.
- Spec, первый рубеж (general-purpose, opus) по замыслу с sampling: 1 противоречие (temperature при
  включённых рассуждениях) и 3 пробела (модель разговора, расход при отмене, служебный агент) —
  все сняты вместе с sampling.
- 2026-09-24: ведущий нашёл, что sampling устарел в протоколе 2026-07-28 (SEP-2577). Пользователь
  выбрал: сервер сам зовёт DeepSeek, ключ в своём файле `~/.config/pipeline-mcp/config.json`
  (600, `$PIPELINE_CONFIG_DIR`). myharness не меняется. Дизайн и план переписаны.
- 2026-09-24: ПЕРЕСМОТРЕННЫЕ ДИЗАЙН И ПЛАН УТВЕРЖДЕНЫ пользователем («да»).
- Spec, первый рубеж (по пересмотренному плану): требований не задевает — раздел плана так и
  говорит; чист одной строкой. Spec → Executing.

## Открытые вопросы
