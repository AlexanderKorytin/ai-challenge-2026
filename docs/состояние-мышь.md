# Состояние работы «своё выделение мышью»

Род работы: **общая задача в `main`** (отложенная задача 1). Ведущая среда: **Claude Code**.
Заведён 2026-09-16 на стадии `Research`.

## Решения пользователя

- 2026-09-16: ведущая среда — Claude Code.

## Открытые вопросы

- (пока нет)

## Ход работы

- 2026-09-16, `Research`: замерами установлено, что посылки отложенной задачи 1 неверны
  (подробности — `docs/выделение-мышью.md`). Пользователь решил: поведение не менять, назвать
  клавишу `Fn` в справке, своё выделение оставить отложенной задачей.
- 2026-09-16: дизайн `docs/выделение-мышью.md` утверждён пользователем («все утвердил — пиши в
  автомате»). Переход `Research → Plan → Executing`.

## План

1. Справка `/help`: назвать клавишу обхода перехвата мыши (`ui.py`, раздел про мышь).
2. Требование `openspec/specs/interface/spec.md`: сценарий выделения привести к правде, добавить
   обязанность справки называть клавишу.
3. `docs/отложенное/список.md`: переписать задачу 1 с верной постановкой и замерами.
4. Проверки: `check_units.py`, `check_app.py`, `openspec validate --specs --strict`; независимый
   просмотр; строка в таблице работ `CLAUDE.md`.

## Круг просмотра, 2026-09-16

Первый независимый просмотр (`general-purpose` на `opus`) дал десять замечаний. Главное —
класс беды был починен не целиком: то же ложное утверждение («клики и выделение уживаются»)
лежало ещё в пяти местах кода и проверок, а `picker.py` вдобавок утверждал обратное коду.
Прочее: противоречие внутри требования и внутри справки, новый сценарий без проверки,
незамеренное про iTerm2, слишком сильное обобщение про образец, транслитерация «бинарник».
Всё снято.

Повторный просмотр только исправлений подтвердил снятие и нашёл новую беду, заведённую самой
правкой: я усилил непроверенное — «при `1003h` выделить текст нельзя ничем», хотя замера
`1003h` с клавишей обхода никто не делал. Причинность убрана из всех пяти описаний и из
требования; теперь сказано только то, что замерено. Заодно сняты: расхождение границ дизайна
со сделанным, ложная причинность в `state.py`, негодная подробность при падении проверки.

**Вердикт стадии `Validation`:** правка принята. Чем подтверждено: `check_units.py`,
`check_app.py`, `check_layers.py` — все проходят; `openspec validate --specs --strict` —
15 из 15; живой прогон в псевдотерминале показал новый текст справки на экране; новая проверка
справки доказано падает на сломанном тексте (подмена «клавишей обхода перехвата» →
«клавишей-модификатором» даёт `ПРОВАЛЕНО: 1`).

## Открытые вопросы

- Своё выделение протяжкой (подход A) остаётся отложенной задачей 1. Возвращаться к ней, если
  клавиши обхода окажется мало.
