# Состояние: планировщик и фоновые задачи (день 18)

Род работы: **задание дня 18** (неделя 4, день 3 → ветка `w4d3`, папка `week_4/day_3/`). Ведущая
среда: **Claude Code**.

## Стадия

Spec, первый рубеж. Дизайн `docs/планировщик.md` утверждён пользователем 2026-09-23 («да»), без подгрузки истории. План `docs/план-планировщик.md` согласован пользователем 2026-09-23 («да»).

## Решения по ходу

- Правка `myharness` не нужна: мост дня 17 уже отдаёт модели разрешённые инструменты `mcp__cbr`.
  Работа только в `mcp_servers/cbr/` (в `main`), ветка дня — после неё.
- Профильный разбор не брался: сервер MCP знаком по дню 17, планировщик сверен с образцами
  (Claude Code `CronCreate`, Hermes `cron`).

## Spec

- Первый рубеж 2026-09-23: чист одной строкой — раздел плана «Изменения требований»: требований
  `myharness` не задевает; проверяющий не звался по правилу соразмерности.

## Executing

- Основа (вход в Executing): `bf08e74`.
- Шаги 1–2 закрыты фиксацией `5af43f0` (вердикт в сообщении).
- Шаг 3: служба выложена на 38.180.117.69, сбор по расписанию на сервере работает (запуски раз в
  минуту, курс USD в базе, после перезапуска задание живо). Пробное задание №1 `* * * * *` ещё
  НЕ снято.
- Шаг 3, беда пути (2026-09-23): с Мака прямые соединения к 38.180.117.69 (HTTPS и ssh)
  замерзают через 2–9 обменов; с самого сервера 40/40; к 83.136.235.149 (FirstVDS, РФ) 40/40
  и 485 КБ на одном соединении. tcpdump на сервере: запрос дошёл, сервер повторяет ответ в 119
  байт, подтверждения от Мака нет — глушится поток сервер→Мак на пути, не MTU и не сервер.
  Отвергнуто: порог по объёму 12–16 КБ (замёрзло после 3,7 КБ мелкими ответами), MTU (пакеты
  по 119 байт тоже теряются). Через туннель Happ стало хуже (ssh закрыт, HTTPS замёрз сразу) —
  адрес возвращён в исключения. Правка маршрута на сервере (`mtu lock`) не делалась — отказ
  автоматического режима; после tcpdump она и не нужна.
- Пользователь предложил переезд на 83.136.235.149; решение о способе — ниже.
- Executing → Research → Executing 2026-09-23: дизайн дополнен разделом «Размещение: переезд на
  83.136.235.149» (решения пользователя: переезд — его предложение; сертификат — «сначала имя,
  иначе IP»). Имя: панель 2026-09-23 снова упёрлась в предел fvds.ru (повторит сама после
  18:43 MSK). Взят IP-сертификат панели за nginx :8443 — файлы входа из `852faa7^`.
- Служба на 38.180.117.69 выключена (`disable --now`), Caddy там остался. Имя ssh
  `challenge-fvds` заведено в `~/.ssh/config`.
- Шаг 3 на 83.136: 40×tools/list на одном соединении — 484 КБ без замерзания; без токена 401;
  пробное задание: 3 удачных запуска, после `restart` задание и курс на месте, снято.
  Рабочее задание №2 USD/EUR/CNY `*/30 * * * *` заведено к видео.
- Шаг 3 независимо НЕ просмотрен — включить в итоговый просмотр Validation (как добавку IP в
  дне 17): модуль службы (StateDirectory, CBR_MCP_DB, allowed-host), вход nginx, vps.md.
- Шаг 4 закрыт в ветке `w4d3` (`1417930`): живой обмен DeepSeek — три вопроса одному агенту на
  одном соединении с паузами, 7 вызовов, замерзаний нет. Независимый просмотр — в итоговом
  просмотре Validation.

## Spec, второй рубеж

- 2026-09-23: чист одной строкой — `git diff --stat bf08e74 -- openspec/specs` пуст.

## Validation
- Итоговый просмотр 2026-09-23 — general-purpose (opus), весь срез `bf08e74..HEAD` и `w4d3`
  (включая непросмотренные шаги 3–4): нового круга не требуется. Находка низкой важности:
  `docs/vps.md` велит смотреть базу командой `sqlite3`, а на 83.136 её нет (рабочий вариант —
  Python из `.venv`). Мелочи: пример `--allowed-host 38.180…` и «через Caddy» в `server.py`;
  нет первичного `enable --now nginx-reload.timer` в доставке; README дня «Стадия: Executing»;
  на 38.180 в выключенной службе осталось задание №1 `* * * * *`.
- 2026-09-23: пользователь заказал третий сервер `188.120.230.58` (чистая Ubuntu 24, без
  панели) — служба переедет туда, устройство как на 38.180 (Caddy с IP-сертификатом, ufw).
  Находку и мелочи просмотра закрыть вместе с переездом. Ждём: адрес в исключения Happ и ключ
  `challenge_vps` через `ssh-copy-id` (сейчас маршрут через utun25, ssh закрыт Happ).
  Полный проход Validation — после переезда.
- 2026-09-23: переезд на 188.120.230.58 (`ssh challenge-mcp`) выполнен: вход ssh только по ключу
  (`10-challenge.conf`, пароль — отказ `publickey`), ufw 22/tcp+443/tcp, Caddy 2.11.4 с
  IP-сертификатом (получен), uv 0.12.18, `/opt/challenge` git, новый токен в `.env`. База
  перенесена с 83.136 копией при остановленной службе (integrity ok, задание №2 и 3 курса).
  Проверка пути: 40×tools/list на одном соединении, 484 КБ; без токена 401; `/` — 404; 80 закрыт.
  На 83.136 служба и `nginx-reload.timer` выключены, входы nginx удалены. `deploy/nginx-*`
  удалены из репозитория, Caddyfile и служба — на новый адрес. Находка итогового просмотра
  (`sqlite3` на сервере) снята: здесь sqlite3 установлен. Изменения переезда (развёртывание и
  документы) независимо не просмотрены — отдельным узким просмотром в Validation.
- Просмотр переезда 2026-09-23 — general-purpose (opus), узкое поручение о рисках: находок по
  существу нет (ssh только по ключу при любых условиях, наружу 22 и 443, iptables без обходов,
  токен вне git и отличается от прежнего, выложенное совпадает с репозиторием, планировщик
  один). Мелочи закрыты: старый токен на 83.136 стёрт, в vps.md — каталог доставки, чужое имя
  узла, 502 старого Caddy на 38.180.
- 2026-09-23: пользователь указал строку результата «Агент, который работает 24/7 и
  периодически выдаёт сводку» — в первой вставке условия её не было. Executing → Research:
  дизайн дополнен разделом «Агент 24/7…» (агент DeepSeek на VPS по таймеру systemd, сводку
  сохраняет программа через `save_digest`, `list_digests` отдаёт). Дизайн и план шагов 5–7
  УТВЕРЖДЕНЫ пользователем 2026-09-23 («утверждаю»). Полный проход Validation выполнен до
  этого (check_server 73/73, openspec 18/18, живой обмен против 188.120) — после шагов 5–7
  его повторить.
- Шаги 5–6: сделаны и выложены (агент запущен вручную — сводка №1; таймер `*:0/10` включён).
  Ключ DeepSeek доставлен трубой: локальный Python печатал его в stdin `ssh`, на сервере
  `read -r k` и встроенный `printf` дописали строку в `.env` — в командной строке процессов и в
  выводе ключа не было. Просмотр шва — general-purpose (opus): (1) сводка без данных
  сохранялась с кодом 0, если набор инструментов не собрался — исправлено отказом при
  `store_error`, при нуле вызовов и при отсутствии записи cbr; (2) зависание навсегда
  останавливало расписание — решение пользователя: `TimeoutStartSec=5min`. Мелочи: журнал
  ошибок записи печатается, в vps.md — цена ручного запуска.
- Шаги 5–6 зафиксированы `e4a6011`; таймер сработал сам в 18:00 (сводка №2, success).
  Шаг 7 — `w4d3`: пересказ сводок через list_digests; просьба записать сводку — отказ, инструмента
  нет. Второй рубеж Spec после шагов 5–7: `openspec/specs` не тронуты — чист одной строкой.
  Validation: нужен новый итоговый просмотр (шаги 5–7 и исправление находки №1) и полный проход.
- Итоговый просмотр после шагов 5–7 — general-purpose (opus): (1) доставка агента оставляла
  старую копию myharness в .venv — `uv sync --reinstall-package myharness` в vps.md; (2) агент
  считал запрошенные вызовы, а не удавшиеся — теперь нужна хотя бы одна удача get_summary /
  get_rate_dynamics / get_rate. Доказано: агент против сервера без курсов — get_summary ×2 с
  ошибкой, код 1, сводок 0. Исправления (`3585fb9`) повторно не просматривались: правка узкая,
  доказана на сломанном случае. Мелочи закрыты (список разрешений в дизайне, комментарий
  store_error, фиксация на сервере `35e2b02`).
- Второй рубеж Spec после исправлений: `openspec/specs` не тронуты — чист.

## Вердикт Validation (2026-09-23)

Полный проход на неподвижном дереве `3585fb9`:
- `cd mcp_servers/cbr && uv run python check_server.py` — код 0, 80/80 «ок», провалов 0;
- `openspec validate --specs --strict` — 18/18 (задача поведения myharness не меняла; наборы
  проверок myharness не запускались — библиотека не правилась);
- службы на 188.120.230.58: `cbr-mcp`, `caddy`, `cbr-digest.timer` — active;
- таймер агента сам в 18:10 на исправленном коде: 5 вызовов, сводка №3, `success`;
- живые обмены DeepSeek из песочницы (шаги 4, 7 и полный проход): завести/снять сбор,
  `list_schedules`, `get_summary`, пересказ `list_digests`, отказ подделать сводку;
- путь с Мака: 40×tools/list на одном соединении, 484 КБ.
Не проверено: окно терминала `myharness` глазами (будет на видео); продление IP-сертификата
Caddy (первое — около 27–28.09); перезагрузка сервера целиком (догон пропуска таймером
`Persistent=true` и сбором — только по проверкам и перезапуску служб).
Расход дня до этой записи: см. `python3 bin/расход.py --день 2026-09-23`.
- 2026-09-23: пользователь подтвердил результат и сценарий видео (шесть пунктов: list-timers и журнал агента справа; list_schedules, get_summary, срабатывание таймера в :X0, list_digests, отказ записать сводку — слева). Validation → Report.
- 2026-09-23: Report — видео (ускорено в 6 раз, обрезано 0:00–1:13 по решению пользователя) на Диске https://drive.google.com/open?id=1Aw8ySfpE_TDmIeY7lmtBE-zEh6-HF3p_, ветка `w4d3` отправлена (`f37905c`). Пункт 6 сценария в ролик не вошёл, в README дня отражено, откуда myharness знает о сервере. **Done.**
