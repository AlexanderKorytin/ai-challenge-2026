# Состояние работы: персонализация ассистента (день 12)

Род работы: **задание дня** (день 12, неделя 3 день 2). Ведущая среда: **Claude Code**.
Решение пользователя 2026-09-15: код `myharness` правится в `main`; папка `week_3/day_2/` и
ветка `w3d2` заводятся под постановку, дизайн, песочницу живого прогона и видеоотчёт — как в
дне 11.

## Условие дня (дословно)

Персонализация поверх модели памяти: создать профиль пользователя, описать предпочтения (стиль,
формат, ограничения), подключить профиль к каждому запросу. Проверить ответы для разных профилей
и что ассистент учитывает профиль автоматически.

## Видение пользователя

- Механизм создания профиля командой вида `/profile new`.
- Человек одним текстом описывает профиль: стиль, формат, ограничения, роль, ход выполнения
  задач, каких помощников с какими ролями вызывать.
- Описание уходит в модель, она заполняет поля. Не названное в описании — доспрашивается
  интервью; на вопрос можно ответить «неважно», и тогда поля нет.
- Профиль сохраняется, инструмент сразу переходит в него.
- Ориентир по составу полей — Claude Code и Codex (прямая просьба пользователя).
- Демонстрация на видео: создаём профиль математика и профиль писателя (философа), задаём один
  общий вопрос обоим.

## Открытые вопросы

- Какой общий вопрос задать математику и философу на видео (подобрать и предложить).
- Чем «профиль пользователя» условия соотносится с действующим профилем генерации (`/profile`)
  и со слоем «человек» модели памяти дня 11 — решается дизайном.

## Согласия и вердикты

- 2026-09-15: выбрана среда Claude Code, род работы — задание дня с веткой `w3d2`.

- 2026-09-15: дизайн `docs/персонализация.md` **утверждён пользователем**. Принят вариант A:
  предпочтения занятия живут в профиле занятия, глобальные факты — только то, что правда всегда
  («глобальная изначально так и задумывалась — правда в любом разговоре», слова пользователя).
- 2026-09-15: вариант B (область действия у факта о человеке) в день 12 НЕ входит — идёт
  отложенной задачей. Туда же — естественная запись в профиль занятия зачином, как у четырёх
  слоёв памяти.
- 2026-09-15: правку инструкции архивариуса в день 12 НЕ вносим — решение пользователя. Вместо
  неё профиль добавляется в правило старшинства записей о человеке (раздел Р0.2 дизайна).
  Принятый риск: тихое подмешивание записи, не противоречащей профилю напрямую. Лечение —
  строка в инструкции архивариуса — идёт отложенной задачей.
- 2026-09-15: пользователь решил включить в день 12 поле профиля `перекрывает` (раздел Р0.3
  дизайна) — перекрытая запись ПОМЕЧАЕТСЯ в блоке, а не выбрасывается. Возврат Plan → Research
  на это уточнение сделан до написания плана.
- 2026-09-15: принято решение пользователя — запись, задающая поведение и не покрытая описанием,
  уходит в ход опроса (раздел Р0.4). Важность решает МОДЕЛЬ по описанию и записям: своего
  критерия и потолка на число вопросов инструмент не вводит — слова пользователя «пусть LLM по
  описанию профиля определит важные сама».
- 2026-09-15: план `docs/план-персонализация.md` (8 шагов) **согласован пользователем**. Переход
  Plan → Executing.
