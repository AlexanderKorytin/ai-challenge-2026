# Состояние: разделение cli.py

Род работы — **общая задача в `main`**. Ведущая среда — **Claude Code**.

## Замер задачи

- Начало: 2026-09-13 20:04 MSK, ветка `main`, вершина `211ca92`.
- Замер расхода в конце: `python3 bin/расход.py --день 2026-09-13`.
- Время выполнения: засекается по этой строке и отметке окончания ниже.

## Согласия пользователя

- 2026-09-13: задача принесена как «разделение cli.py по новому конвейеру с замером токенов и
  времени выполнения».
- 2026-09-13 20:0x: `cli` НЕ остаётся фасадом — честное разделение, проверки переводятся на
  новые имена модулей (выбор пользователя из двух предложенных).
- 2026-09-13: дизайн `docs/разделение-cli.md` утверждён пользователем словами «да, утверждаю».
  Стадия Research закрыта.

- 2026-09-13: план `docs/план-разделение-cli.md` (15 шагов, все ходом быстрый) согласован
  пользователем словами «да». Стадия Plan закрыта, начата Executing.


## Решения и находки по ходу

- 2026-09-13, шаг 5: константа `STRATEGY_TITLES` (её в плане не было — она не определение, а
  словарь уровня модуля) переехала в `strategies.py`: названия режимов принадлежат режимам.
- Шаг 15 сделан отдельно от разделения (другой файл, по плану ни от чего не зависит):
  `bin/расход.py` печатает столбец «время» — протяжённость каждого дня — и строку «время
  работы: <начало> → <конец>». Время по задаче складывается из дневных протяжённостей, а не
  берётся календарной разницей: между днями работа стоит.

- 2026-09-13, просмотр шва шагов 1–5 (помощник на opus): поведение подтверждено посимвольным
  сличением всех 80 определений, 6 настоящих замечаний — все про правдивость пояснений и уборку.
  Главное: `state.py` обещал лёгкость, а тянул весь терминал через `archivist`/`compact`,
  ввезённых ради двух строк с именами служб. Исправлено переездом имён в лёгкий `background`
  (`ИМЯ_АРХИВАРИУСА`, `ИМЯ_СЖИМАТЕЛЯ`) — это единственная правка сверх переноса за весь шов, и
  сделана она, чтобы обещание стало правдой, а не чтобы убрать обещание. Утверждение закреплено
  парой проверок в отдельном процессе: состояние терминала не тянет, тот же опрос по `cli` его
  находит.
- 2026-09-13: мёртвая `panes.active_strategy` удалена, а не перенесена: её не звали ни код, ни
  проверки ни до разделения, ни после.

- 2026-09-13, повторный просмотр исправлений (помощник на opus): 3 настоящих замечания, все про
  пояснения. Главное: после переезда имён служб круга `state → службы` больше нет, а пояснения
  его всё ещё обещали. Исправлено по плану — `archivist`, `compact` и `output` ввозят состояние
  обычным образом, подсказки типа сняты. Проверка «имя состояния взято только для подсказок
  типов» заменена на два утверждения по существу: архивариус ввозит состояние обычно, и
  состояние не ввозит служб — направление слоёв держится именно этим.

- 2026-09-13, шаги 6–11: заведены два вспомогательных разборщика в песочнице — «свободные
  имена» (что в модуле не разрешается) и «мёртвые ввозы». Первый сразу нашёл потерю, которую
  проверки не ловили: в `workers.py` не был ввезён `Any`, и молчало это только из-за отложенных
  пояснений типов. Разбор дерева дешевле просмотра и ловит другое — держать оба.

- 2026-09-13, просмотр шва шагов 12–14 (помощник на opus): деление `build_app` подтверждено
  посимвольно — расхождений два и оба заявленные; порядок побочных действий и все привязки
  клавиш с их условиями сохранены. 2 настоящих замечания: таблица слоёв в `README.md` не
  соответствовала настоящему порядку ввозов (переписана по топологическому порядку, снятому
  разбором) и приписывала проверке больше, чем та проверяет (утверждения разведены); докстрока
  `layout.py` обещала, что окно живёт столько же, сколько панель, — на деле окна не убираются
  никогда, и это известный изъян, записанный отдельной работой.
- 2026-09-13: договор `Раскладка` сужен с семи полей до пяти — `input_area` и `занятость` не
  читал никто.

## Открытые вопросы

- **Двойной словарь названий режимов.** `strategies.STRATEGY_TITLES` и
  `ui.CONTEXT_STRATEGY_TITLES` — одна и та же таблица названий, записанная дважды и уже разными
  способами (первая через константы `context_strategy`, вторая строками). Это техдолг, но он
  старше этой задачи, и его слияние — правка поведения показа, а не перенос. Предлагаю
  отдельной точечной задачей; молча внутри разделения не делаю.
