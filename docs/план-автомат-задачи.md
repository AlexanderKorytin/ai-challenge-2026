# План: автомат состояний задачи (день 13)

Дизайн: `docs/автомат-задачи.md` (утверждён 2026-09-16). Состояние: `docs/состояние-автомат-задачи.md`.
Код и требования — в `main`; ветка `w3d3` и папка `week_3/day_3/` — шагом 10, после кода.
Пути кода ниже — от `myharness/src/myharness/`, проверок — от `myharness/tests/`.

## Карта файлов

| Файл | Отвечает за |
|---|---|
| `api.py` | поток собирает куски вызовов инструментов; `tools` в запросе |
| `memory.py` | запись ответа несёт приложение (рассуждения и звенья круга); чтение отдаёт приложения |
| `tokens.py` | вес сообщения считает вызовы и рассуждения; вес описаний инструментов |
| `agent.py` | память пар с приложением; раскрытие пары; круг «вызов → результат → продолжение» |
| `conversation.py` | подъём разговора передаёт приложения агенту |
| `machine.py` (новый) | карта стадий: разбор, проверка, переходы; три инструмента автомата |
| `profiles.py` | поле `стадии` в профиле: чтение, слепок, запись |
| `workspace.py` | новые ключи заголовка задачи; план со статусами; блок состояния с картой |
| `state.py` | поставщик инструментов главному агенту |
| `team.py` | `созвать` — ответы группы без сводки |
| `commands_task.py` | `/task утвердить`, `отклонить`, `пауза`, `продолжить`; карта в `new` и `этап` |
| `output.py`, `ui.py` | круги рассуждений, строка вызова, стадия и «ждёт» под вводом |
| `profile_maker.py` | мастер возвращает карту стадий |
| `openspec/specs/*`, новая `openspec/specs/task-machine/` | требования — каждый шаг правит свои |
| `tests/check_units.py`, `tests/check_layers.py` | целевые проверки шагов |

## Договоры модулей

### api
**Владеет**: ничем, кроме соединения.
**Отдаёт**: `stream_chat(model, messages, params=None, tools: list[dict] | None = None) -> AsyncIterator[StreamEvent]`;
`StreamEvent.kind` — `"reasoning" | "content" | "tool_calls" | "meta"`; новое поле
`StreamEvent.calls: list[dict]` — каждый элемент `{"id": str, "type": "function",
"function": {"name": str, "arguments": str}}`.
**Обещает**: событие `"tool_calls"` приходит не больше одного раза за поток, после всех
кусков текста и до `"meta"`; куски склеены по `index`; `tools=None` или `[]` — в запрос
`tools` не уходит вовсе, поведение байт в байт прежнее.
**Не делает**: доводов не разбирает (строка JSON как пришла), инструментов не исполняет.

### memory (запись разговора)
**Владеет**: файлом разговора.
**Отдаёт**: `SessionStore.append_pair(user, assistant, meta=None, *, приложение: dict | None = None) -> str | None`;
`Restored.приложения: list[dict]` — той же длины и в том же порядке, что `Restored.pairs`
(окно режет оба одинаково), пустой словарь у пары без приложения.
**Обещает**: приложение — словарь с ключами `reasoning_content: str` и `звенья: list[dict]`
(сообщения круга: ответ модели с `tool_calls` и своим `reasoning_content`, результаты с ролью
`tool`); оба пишутся ПОЛЯМИ записи ответа, отдельных строк не появляется; запись без этих полей
читается как пара без приложения; поле неверного вида отбрасывается с предупреждением, пара
остаётся.
**Не делает**: не проверяет смысл звеньев.

### agent
**Отдаёт**:
- `@dataclass Инструменты: схемы: list[dict]; исполнить: Callable[[str, str], Awaitable[str]]` —
  имя инструмента и строка доводов на вход, текст результата на выход;
- `Agent(..., инструменты: Callable[[], Инструменты | None] | None = None)`;
- `remember(question, answer, *, model="", приложение: dict | None = None)`;
- `restore(pairs, приложения: list[dict] | None = None)`;
- `раскрыть(сообщения: list[dict], полные: bool) -> list[dict]` (функция модуля);
- `Turn.звенья: list[dict]`.

**Обещает**:
- `_messages` по-прежнему строго чередует `user`/`assistant`, приложение живёт ключом `"приложение"`
  у сообщения ответа и никогда не уходит в API как есть;
- `раскрыть(..., полные=False)` отдаёт `{"role", "content"}` и ничего больше;
- `раскрыть(..., полные=True)` отдаёт вопрос, звенья, ответ с `reasoning_content` — ключ
  ставится, только если рассуждения записаны (итог шага 1);
- вес памяти (`_preview`, `history_tokens`, `подобрать_с_конца`) всегда считается по полному
  раскрытию;
- инструменты запрашиваются у поставщика один раз за обмен; `None` — обмен как прежде;
- исключение исполнителя становится текстом результата «ошибка инструмента: …», круг
  продолжается;
- неотвеченный или отменённый обмен не оседает в памяти ни вопросом, ни звеньями.

**Не делает**: смысла инструментов не знает; предела числа кругов не ставит.

### machine
**Владеет**: ничем на диске сам — пишет через `workspace`.
**Отдаёт**:
- `@dataclass(frozen=True) Стадия: имя: str; цель: str; утверждение: bool; консилиум: tuple[str, ...]; дальше: tuple[str, ...]`;
- `@dataclass(frozen=True) Карта: стадии: tuple[Стадия, ...]` с методами `стадия(имя) -> Стадия | None`,
  `начальная -> Стадия` (свойство), `конечная(имя) -> bool`;
- `имя_стадии(текст: str) -> str` — единственное приведение имени стадии (`" ".join(split()).upper()`), его же зовёт `workspace.set_stage`;
- `разобрать_карту(raw: Any, есть_профиль: Callable[[str], bool], *, свой_профиль: str | tuple[str, ...] = "") -> tuple[Карта | None, list[str]]`;
- `в_список(карта: Карта) -> list[dict]` — обратное разбору, для записи профиля;
- `СХЕМЫ: list[dict]` — описания трёх инструментов;
- `набор(state) -> Инструменты | None`.

**Обещает**: имя стадии приводится `имя_стадии`; переход в неизвестную стадию и повтор имени
отбрасываются с предупреждением; неизвестный профиль консилиума ОСТАЁТСЯ в карте с
предупреждением и проверяется при созыве (шаг 6); сам профиль в консилиуме отбрасывается;
стадия, у которой были переходы, но ни одного годного не осталось, делает негодной всю карту;
карта без конечной стадии или без единой стадии — `None` и предупреждение; `набор` отдаёт
`None`, если нет хотя бы одного из трёх условий Р9 (профиль с картой, открытая задача этого
профиля, не пауза) или стратегия профиля `branching`.

**Не делает**: не распознаёт согласия; не применяет переход через ворота.

### team
**Отдаёт**: `async созвать(state, вопрос: str, имена: list[str]) -> list[tuple[str, str]]` —
пары «имя профиля, текст ответа» только ответивших.
**Обещает**: запросы одновременные, панели исполнителей видны, не ответившие названы вслух;
`run` пользуется `созвать` и ведёт себя как прежде.

## Шаг 1. Живая разведка вызова инструментов DeepSeek

**Файлы**: скрипт в песочнице ведущего (не в репозитории); итог — строками в `docs/состояние-автомат-задачи.md`
**Зависит от**: ничего
**Ход**: ведущий сам — это замер, а не код инструмента
**Требование**: не про myharness
**Проверка**: скрипт на `deepseek-v4-flash` с рассуждениями и одним инструментом отвечает на четыре вопроса
**Содержание**: пять запросов напрямую через `openai.AsyncOpenAI`:
1. вид кусков `delta.tool_calls` в потоке и `finish_reason` при вызове;
2. есть ли текст (`content`) в ответе с вызовом;
3. отказывает ли сервер, если у прошлого ответа модели нет `reasoning_content` при `tools` в запросе
   (так выглядят пары, записанные до этого дня), и помогает ли пустая строка;
4. отказывает ли сервер, если `tools` в соседних запросах то есть, то нет (пауза снимает инструменты).
Ответ на пункт 3 решает, что `раскрыть(полные=True)` кладёт в старую пару: пустую строку или
пара без рассуждений раскрывается без инструментов вовсе. Решение записывается в файл состояния
до шага 2.

## Шаг 2. Пара несёт приложение и переживает перезапуск

**Файлы**: `memory.py`, `tokens.py`, `agent.py` (только `remember`, `restore`, `раскрыть`, `_preview`, `history_tokens`, сборка `selected_messages` в `build_messages`), `conversation.py`, `openspec/specs/memory/spec.md`, `openspec/specs/agent/spec.md`, `openspec/specs/tokens/spec.md`, `tests/check_units.py`
**Зависит от**: шаг 1
**Ход**: полный, потому что файл разговора читают прежние версии кода и подъём при запуске, а вид памяти читает круг шага 3; и это именованное исключение — восстановление после обрыва записи
**Требование**: `memory` → «Разговор пишется дописыванием дословно», «Испорченная запись не роняет восстановление»; `agent` → «Память выдаётся копией и пополняется единственным способом»; `tokens` → «Вес запроса известен до отправки»
**Проверка**: пара с приложением записана, файл прочитан заново — приложение на месте и в том же порядке при окне 2 из 3 пар; парная проверка: пара без приложения читается с пустым словарём; `build_messages` без инструментов не несёт ни одного ключа, кроме `role` и `content` (предусловие: в памяти лежит пара с приложением)
**Содержание**:
- `append_pair`: `приложение` кладётся полями `reasoning_content` и `звенья` в запись ответа, после `meta` и не перекрывая служебные поля.
- `read_session`: поле `звенья` — список словарей, у каждого `role` из `assistant`/`tool`; иначе предупреждение «строка N: звенья испорчены — пара без них» и пустое приложение. `reasoning_content` не строка — так же.
- `Agent.remember`: `приложение` ложится ключом `"приложение"` в сообщение ответа только если непусто и передаётся в `append_pair`. Стратегия `branching` приложение не пишет (Р9: инструментов там нет).
- `раскрыть` вызывается ровно в одном месте `build_messages` — над `selected_messages` обеих ветвей; `полные` в этом шаге всегда `False`, признак подаёт шаг 3.
- `_preview`, `history_tokens` и `подобрать_с_конца` взвешивают ПОЛНОЕ раскрытие (`_вес_памяти`) при любом `полные` — перевес при паузе безопасен.
- `tokens.count_messages`: к `content` прибавляются `count_text(reasoning_content)` и `count_text(json.dumps(tool_calls, ensure_ascii=False))`, если они есть; новая `count_tools(схемы) -> int` — `count_text` от JSON схем.
- `conversation.restore_conversation` передаёт `restored.приложения` в `agent.restore`.

## Шаг 3. Круг вызовов инструментов внутри обмена

**Файлы**: `api.py`, `agent.py` (`Инструменты`, `Turn.звенья`, `exchange`, признак `полные` в `build_messages`), `openspec/specs/api/spec.md`, `openspec/specs/agent/spec.md`, `openspec/specs/params/spec.md`, `tests/check_units.py`
**Зависит от**: шаг 2
**Ход**: полный, потому что вид событий потока читает отрисовка (шаг 8), а договор `Инструменты` — автомат (шаг 5)
**Требование**: `api` → «Потоковый ответ разделяет рассуждения и ответ»; `agent` → «Пустой ответ не считается успехом», новое «Круг вызовов инструментов идёт внутри одного обмена»
**Проверка**: поддельный клиент отдаёт сначала вызов, потом текст — в памяти одна пара, у ответа два звена, в журнале `звенья`, `usage` сложен из двух запросов; второй запрос несёт `tools`, звенья и `reasoning_content`; парная проверка: поставщик вернул `None` — в запросе нет `tools`, круг не начинается; отмена во время исполнения инструмента — памяти не прибавилось
**Содержание**:
- `api.stream_chat`: куски `delta.tool_calls` копятся словарём по `index` (`id`, `function.name`, склейка `function.arguments`), одно событие `"tool_calls"` перед `"meta"`.
- `exchange`: `набор = self._инструменты() if self._инструменты else None` один раз; `build_messages(content, model, полные=набор is not None)`; `params` запроса — профиля плюс `tools=набор.схемы`. Круг: пока поток кончился событием `"tool_calls"` — звено ответа `{"role": "assistant", "content": текст круга, "reasoning_content": рассуждения круга, "tool_calls": calls}`, затем по каждому вызову `await набор.исполнить(name, arguments)` и звено `{"role": "tool", "tool_call_id": id, "content": результат}`; звенья дописываются к `request_messages`, поток запускается заново. `usage` кругов складывается `tokens.add_usage`. `on_event` получает и новое событие.
- Пустой ответ: ошибкой считается только итоговый круг без текста и без вызовов.
- Успех: `remember(content, answer_text, model=model, приложение={"reasoning_content": рассуждения итогового круга, "звенья": звенья})`, если инструменты были в запросе.
- `predict_tokens` прибавляет `tokens.count_tools(набор.схемы)`.
- `_over_budget` обычной стратегии считается по собранному списку `messages` в конце `build_messages`, как у строгих, — иначе на паузе человеку говорят «запрос тяжелее предела» по полному весу, хотя ушёл голый.
- Журнал: `entry["звенья"]`, если непусто.

## Шаг 4. Карта стадий в профиле

**Файлы**: `machine.py` (новый: `Стадия`, `Карта`, `разобрать_карту`, `в_список`), `profiles.py`, `openspec/specs/profiles/spec.md`, `tests/check_units.py`
**Зависит от**: ничего
**Ход**: полный, потому что карту читают автомат (шаг 5), команды (шаг 7) и мастер (шаг 9), а профиль с картой ложится на диск
**Требование**: `profiles` → новое «Профиль задаёт карту стадий», «Мусор в профиле не роняет профиль», «Слепок профиля для журнала»
**Проверка**: профиль с картой конвейера (пять стадий плюс `DONE`) читается, `стадия("research").дальше == ("PLAN", "EXECUTING")`; переход в неизвестную стадию и консилиум из несуществующего профиля отброшены с предупреждениями, профиль загружен; парная проверка: профиль без поля `стадии` — `Profile.стадии is None`, поведение прежнее; запись `save_pair` и чтение дают равную карту
**Содержание**:
- `Profile.стадии: Карта | None = None`; json-ключ `"стадии"` в `_from_dict` (в перечень известных ключей), в слепок и в запись — через `в_список`.
- `есть_профиль` для разбора — проверка по списку имён профилей (`profiles.list` уже есть); сам профиль в консилиуме себя не называет — отбрасывается.
- Элемент карты: `имя` (обязательно), `цель` (строка, по умолчанию пусто), `утверждение` (bool), `консилиум` (список имён), `дальше` (список имён). Стадия без `дальше` или с пустым списком — конечная.

## Шаг 5. Автомат ведёт задачу: план со статусами и переходы

**Файлы**: `machine.py` (`СХЕМЫ`, `набор`, исполнители `update_plan` и `move_stage`), `workspace.py`, `state.py`, новая `openspec/specs/task-machine/spec.md`, `openspec/specs/memory/spec.md`, `tests/check_units.py`
**Зависит от**: шаги 3, 4
**Ход**: полный, потому что доводы модели ложатся на диск в файл, который правит человек, и файл читает прежний код дня 11
**Требование**: новая `task-machine` → «Переход проверяется картой», «Ворота открывает только человек», «Пауза снимает инструменты», «Продолжение после перезапуска»; `memory` → «Рабочая память задачи лежит файлом…» (новые ключи, снятие оговорки о карте)
**Проверка**:
- на задаче в `RESEARCH` вызов `move_stage` в `PLAN` — этап не сменился, `ждёт: человек`, `предложено: PLAN`, результат говорит «ждёт утверждения»; парная: из `PLAN` в `EXECUTING` при `утверждение: false` у `PLAN` в тестовой карте — этап сменился;
- `move_stage` в `DONE` из `RESEARCH` — отказ «нет такого перехода», файл не изменён;
- `update_plan` с двумя `идёт` — отказ; с одним — «План» со статусами, «Сейчас» — этот шаг;
- довод со строкой `"шаг\n## Чужой раздел"` не порождает раздела;
- после сброса памяти агента первый запрос несёт в блоке стадию, цель, допустимые переходы, шаг, «ждёт», «ожидается»;
- **живой обмен** — скрипт ведущего на `Agent` с настоящим ключом и `набор` из этого шага, без окна: «составь план из трёх шагов и предложи переход» — в файле задачи план со статусами и `предложено`.
**Содержание**:
- `Задача`: поля `профиль: str = ""`, `ждёт: str = ""`, `ожидается: str = ""`, `предложено: str = ""`, `пауза: bool = False`; ключи заголовка `профиль`, `ждёт`, `ожидается`, `предложено`, `пауза` (`да`); пустые в файл не пишутся.
- `workspace.обновить(задача, **поля) -> tuple[bool, str]` — снимок, правка полей, `_записать`; строки проходят `" ".join(текст.split())`.
- `workspace.записать_план(задача, шаги: list[tuple[str, str]]) -> tuple[bool, str]` — «План» строками `[ ] шаг` / `[>] шаг` / `[x] шаг`, «Сейчас» — шаг `[>]`, сделанные дописываются в «Сделано» без повторов.
- `workspace.блок(задача, карта: Карта | None = None)` — при карте после строки этапа: «Цель стадии: …», «Дальше можно: …», «Ждёт: человек — …» или «Ожидается: …», «Задача на паузе», и строка «Есть «Сейчас» — продолжай с него, не начинай заново».
- `набор(state)`: профиль главного экрана, задача с `профиль == имя профиля`, не пауза, стратегия не `branching`. `исполнить` разбирает JSON доводов; разбор не удался — результат «доводы не разобраны: …».
- `move_stage(стадия, итог)`: при `ждёт: человек` — отказ; стадия не в `дальше` — отказ; текущая стадия с `утверждение` — одной записью `ждёт=человек`, `предложено`, `ожидается`, `итог` (ключ заголовка), результат «ждёт утверждения человека»; иначе переход применяется: этап, «Сделано» с итогом, незакрытые шаги в «Находки», «План» и «Сейчас» очищены, ключи ворот сняты. Незакрытые шаги называются в результате в обоих путях.
- Инструкция автомата — описания схем; в системную часть ничего не добавляется, кроме того, что сами схемы несут.
- `state.главный_агент` передаёт `инструменты=lambda: machine.набор(state)`.
- Строка в ленту о каждом вызове — через шаг 8; здесь исполнитель возвращает текст результата.

## Шаг 6. Консилиум

**Файлы**: `team.py`, `machine.py` (исполнитель `call_council`), `openspec/specs/team/spec.md`, `openspec/specs/task-machine/spec.md`, `tests/check_units.py`
**Зависит от**: шаг 5
**Ход**: полный, потому что `созвать` читают и `team.run` (прежнее поведение группы), и автомат
**Требование**: `team` → новое «Консилиум возвращает ответы вызвавшей модели без сводки»; `task-machine` → «Консилиум созывается только в своей стадии»
**Проверка**: поддельный клиент, два профиля консилиума — результат инструмента содержит оба ответа с именами, сводного запроса нет; парная: в стадии без консилиума — отказ «в этой стадии консилиума нет»; `/team` работает как прежде (существующая проверка группы)
**Содержание**: `созвать` — выделенная из `run` часть до сводки; `run` зовёт её. Результат инструмента: `Ответ «<имя>»:\n<текст>` через пустую строку; ни одного ответа — «консилиум не ответил».

## Шаг 7. Команды человека

**Файлы**: `commands_task.py`, `machine.py` (`применить_переход`, перевод `move_stage` на неё), `ui.py` (только `task_fragments`), `openspec/specs/commands/spec.md`, `openspec/specs/task-machine/spec.md`, `tests/check_layers.py`, `tests/check_units.py`
**Зависит от**: шаг 5
**Ход**: полный, потому что `применить_переход` меняет путь записи, которым пользуется автомат шага 5, а ворота — главный договор задачи
**Требование**: `commands` → «/task утвердить», «/task отклонить», «/task пауза и продолжить»
**Проверка**: через `cli.repl`: `/task new` в профиле с картой — этап `RESEARCH`, `профиль` записан; `/task утвердить` без предложенного — отказ; с предложенным — этап `PLAN`, `ждёт` снят; `/task отклонить нужен второй вариант` — этап прежний, замечание в «Находки», `ждёт: модель`; `/task пауза` — отметка стоит, `набор` отдаёт `None`; `/task продолжить` — снята; `/task этап DONE` — вне переходов, но в карте: применяется со строкой «переход вне карты сделан человеком»; `/task этап ЛЕС` при карте — отказ со списком стадий
**Содержание**: слова `утвердить`, `отклонить`, `пауза`, `продолжить` в `СЛОВА_КОМАНДЫ` и `ПОДСКАЗКА`; `new` при карте ставит `этап=карта.начальная.имя`, `профиль=state.profile.name`; профиль без карты — поведение дня 11.
- Общая функция `machine.применить_переход(задача, карта, стадия, итог) -> tuple[bool, str]` — одна запись: этап, «стадия A: итог» в «Сделано», незакрытые шаги в «Находки», очистка «План»/«Сейчас», снятие `ждёт`/`предложено`/`ожидается`/`итог`. Её зовут и `move_stage` без ворот, и `/task утвердить` — два пути не расходятся.
- `/task утвердить`: нет `предложено` — отказ; профиль задачи не текущий или карты нет — отказ; `предложено` заново сверяется с картой (`дальше` текущей стадии; его могли поправить руками) — не сходится — отказ с объяснением; иначе `применить_переход(..., предложено, итог)`.
- `/task отклонить <замечание>`: нужно `предложено`; одной записью снимаются `предложено` и `итог`, `ждёт=модель`, `ожидается="переделать итог стадии A по замечанию: …"`, замечание в «Находки» строкой «отклонено: …». Этап прежний.
- `/task пауза` / `продолжить` — `обновить(пауза=True/False)`.
- `/task этап X` при карте: X не из карты — отказ со списком; из карты, но не из `дальше` — применяется (`set_stage`) со строкой «переход вне карты сделан человеком».
- Показ `/task` (`ui.task_fragments`, файл `ui.py` — только эта функция): строка «ждёт: …», «предложено: …», «итог на утверждении: …», пауза — если есть.

## Шаг 8. Экран: круги, вызовы, стадия под вводом

**Файлы**: `output.py`, `ui.py`, `openspec/specs/interface/spec.md`, `tests/check_app.py`
**Зависит от**: шаги 3, 5
**Ход**: быстрый — читатель только человек
**Требование**: `interface` → «Размышления показываются свёрнутой областью» (по области на круг), новое «Вызов инструмента виден строкой», «Стадия задачи видна под вводом»
**Проверка**: ручной сценарий ведущего в окне: ответ с вызовом показывает два свёрнутых заголовка, строку `⚙ move_stage → PLAN: ждёт утверждения` и ответ; `Ctrl+R` разворачивает оба; под вводом `RESEARCH · ждёт вас: утвердить итог стадии RESEARCH · /task утвердить`; целевая проверка в `check_app.py` на порядок строк
**Содержание**: `draw_event` на `"tool_calls"` закрывает текущий заголовок рассуждений (`_finish_reasoning_head`), печатает строку вызова и сбрасывает `marks["reasoning"]`, `marks["answer"]`; результат исполнения печатается исполнителем через `append_log` на панель главного экрана.

## Шаг 9. Мастер `/profile new` заводит карту

**Файлы**: `profile_maker.py`, `openspec/specs/commands/spec.md`, `tests/check_units.py`
**Зависит от**: шаг 4
**Ход**: быстрый — новое поле пишется уже проверенной `save_pair` и читается разбором шага 4, нового договора нет
**Требование**: `commands` → «/profile new» (карта стадий из описания)
**Проверка**: поддельный ответ сборщика с полем `стадии` — профиль записан с картой, чтение даёт ту же карту; парная: у стадии годный переход и переход в неизвестную стадию — второй отброшен вслух, профиль записан с картой; единственный переход с опечаткой — профиль записан без карты, об этом сказано
**Находка живого прогона 2026-09-16**: на описании сценариста со стадиями и консилиумом сборщик положил консилиум в `агенты` — профиль отправлял бы группе КАЖДЫЙ вопрос. При наличии стадий помощники стадии идут в `council` своей стадии. Шире: `агенты` заполняются, ТОЛЬКО если человек прямо попросил отправлять каждый вопрос группе, — упоминание другой профессии в описании помощником не делает (второй случай: профиль «продюсер» получил `agents: [режиссёр]` из фразы «не решать за режиссёра»). Проверка: поддельный ответ сборщика со стадиями и консилиумом — `agents` профиля пуст, `council` у RESEARCH есть.
**Содержание**: в `СБОРКА_ИНСТРУКЦИЯ` — поле `"stages"`: заводится, только если описание или ответы называют стадии и переходы; консилиум — только из переданного списка профилей; пример в json-образце. `Черновик.стадии: Any = None`; `собрать` разбирает через `machine.разобрать_карту` и печатает жалобы.

## Шаг 10. Ветка и папка дня

**Файлы**: `week_3/day_3/README.md`, `week_3/day_3/test/` (ветка `w3d3`)
**Зависит от**: шаги 2–9 зафиксированы в `main`
**Ход**: ведущий, механика
**Требование**: не про myharness
**Проверка**: `git worktree list` показывает `w3d3`; README содержит постановку, видение и ссылку на дизайн
**Содержание**: `git worktree add ~/challenge/w3d3 -b w3d3 main`; README с условием дня; песочница `test/`. Профили консилиума и «разработчика» заводятся на стадии `Validation` живым проходом через `/profile new` — в личный каталог `~/.config/myharness/profiles`, раз профиль глобальный.

## Самопроверка

- Покрытие дизайна: Р1–Р2 — шаги 5, 6, 7; Р3 — 4; Р4 — 9; Р5 — 5, 7; Р6 — 5, 7; Р7 — 5; Р8 — 2, 3, 8; Р9 — 5; риски: вес — 2, 3; доводы на диск — 5.
- Живой обмен несёт шаг 5; он зависит от 3 и 4, ни один из них не интерфейс. Первый живой контакт с сервером — шаг 1.
- Полный проход проверок, итоговый просмотр и прогон с паузой на каждой стадии — стадия `Validation`, не шаг.
- Новых путей записи на диск нет: файл задачи и профили пишутся прежними путями с прежними переопределениями (`MYHARNESS_STATE_DIR`, `MYHARNESS_PROFILES`).
- Одновременно можно вести шаги 4 и 1; 6, 7, 8, 9 — после своих зависимостей.
