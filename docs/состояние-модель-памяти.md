# Состояние: явная модель памяти агента

**Род работы:** задание дня 11 (2026-09-14). Код — в `main`; ветка `w3d1` и папка `week_3/day_1/`
заводятся после исполнения, только под демонстрацию и видеоотчёт.
**Ведущая среда:** Claude Code.

Файл несёт то, чего нет в дереве. Дизайн — `docs/модель-памяти.md`, план — `docs/план-модель-памяти.md`.

## Согласия пользователя

- 2026-09-14: персоны — наставник по программированию и преподаватель испанского, обе с интервью.
- 2026-09-14: интервью устроено как запрос к модели «что уточнить» → вопросы человеку → занесение
  фактов.
- 2026-09-14: факты не суммаризируются; остальной диалог — может.
- 2026-09-14: проверка ответа кодом («скрипт сравнения») — отдельным днём этой недели, не сегодня.
- 2026-09-14: раскладка слоёв задана примером пользователя — четыре хранилища: разговор,
  рабочее (задача), проектное (папка), глобальное (человек). Интервью заполняет карточку **проекта**,
  а не персоны. Это третья редакция дизайна.
- **2026-09-14: дизайн утверждён** («в остальном хорошо — фиксируй дизайн»).
- 2026-09-14: предложение делить записи по профилям (программирование / испанский / питание)
  разобрано и отражено в дизайне открытым вопросом: разделение несёт папка, а не профиль;
  при живой протечке вводится тема у записи плюс список тем у профиля.
- **2026-09-14: план утверждён заранее и без показа** — «план я утверждать не буду, пиши и
  реализуй сразу, считай утвердил, я потом живьём посмотрю». Там же: «дальше в автомате — я приду
  для живой проверки и записи видео» и «если какие-то развилки — ориентируйся на claude code».
  План лежит файлом `docs/план-модель-памяти.md`: он нужен не для утверждения, а чтобы работа
  пережила рубеж очистки.

## Устные решения по ходу

- Нумерация: день 10 — это `w2d5` (стратегии контекста, 2026-09-11); сегодня день 11, ветка `w3d1`.
- В таблице заданий `CLAUDE.md` отсутствует строка задания 10 — дописать в стадии Report.
- «Профиль» не переизобретаем: персона — это обычный профиль генерации, и новых полей у него не
  появляется (поле `persona` из второй редакции отменено вместе с карточкой персоны).
- Деление долговременной памяти по охвату («кому запись нужна») отвергнуто разбором трёх помощников
  и возражением пользователя: машинного признака у такой границы нет, промах разметки тихий.
  Возврат к охвату — только по названному порогу: блок записей перерос инструкцию персоны либо
  записей стало больше полутора сотен.

### Решения стадии Executing (2026-09-14)

- **Общая атомарная запись вынесена в `memory.write_private`** (черновик рядом, права черновику,
  подмена имени) и переиспользуется тремя хранилищами. В плане её не было; дублировать запись в
  трёх модулях — хуже. Заодно `_слаг` → `слаг`, `_make_private_dir` → `make_private_dir`.
- **Пункты всех разделов пишутся одним знаком «-»**, нумерация плана из примера дизайна отменена:
  два правила разбора одного формата разошлись бы, а нумеровать можно при показе.
- **Раздел «Ограничения» не принимается от модели вовсе** — строже дизайна, где интервью вправе
  писать карточку целиком. Причина названа просмотром: повторное интервью затирало записанное
  человеком. Теперь запрет стоит в трёх местах — инструкция второго запроса, `parse_card`
  (единственная дверь ответа модели) и `create` (переносит прежний раздел). Заполняет его
  человек, и инструмент об этом просит после интервью.
- **Правка человека вне известной формы переживает запись**: чужие ключи заголовка и чужие
  разделы хранятся в `Задача`/`Карточка` и дописываются в хвост файла; в блок для модели не идут.
  Следствие того же просмотра: файл правится руками, и молча стирать написанное нельзя.
- **Имя файла задачи — слаг плюс отпечаток названия**, как у записей: слаг режется по 80 байтам,
  и два длинных названия, различных в хвосте, давали один файл.
- **Активная задача при запуске — последняя изменённая** (образец Claude Code: состояние берётся
  из того, что лежит на диске, а не из второго списка в настройках).
- **Шаг 5 затронул сверх плана `strategies.py` и `cli.py`**: главный агент заводится в двух
  местах (запуск и смена профиля), и набор слоёв у них обязан быть один — заведена общая
  `state.главный_агент`.
- **Строка употребления над записями утяжелила запрос примерно на 30 токенов** — замер исполнителя
  шага 4. Обрезка на профилях с малым `budget_tokens` теперь срабатывает чуть раньше; поведение
  верное, но названо вслух.
- **Перенос старой глобальной памяти в архив отложен к шагу 14** (перед демонстрацией): пока идёт
  отладка, непустая память полезнее пустой.

### Решения по итогам независимых просмотров (2026-09-14)

- **Активная задача хранится ИМЕНЕМ, а не снимком.** Просмотр нашёл блокирующее: команда писала
  файл целиком из снимка, снятого при выборе задачи, и затирала правку, сделанную человеком
  руками, — молча и в том самом файле, который задуман как правимый руками. Теперь `State`
  держит `слаг_задачи`, а тело читается с диска перед каждой правкой и перед показом, как
  карточка. Покрыто проверкой в живом приложении.
- **Раздел «Ограничения» не принимается от модели вовсе** — сверх дизайна, где интервью вправе
  писать карточку целиком. Запрет стоит в трёх местах: инструкция, `parse_card`, `create`.
- **Прежняя карточка сохраняется рядом** (`проект.прежняя.md`) перед любой заменой, и если её не
  удалось сохранить, замена не делается. Причина: интервью заменяет карточку целиком, и хорошая
  карточка из пяти разделов могла уехать в никуда ради одного сомнительного раздела.
- **Карточка при формальных придирках всё равно записывается**, придирки печатаются над ней.
  План требовал обратного; выбран дизайн плюс довод исполнителя: придирки — признаки, а не
  приговор, а незаписанная карточка выбрасывает два оплаченных запроса и ответы человека.
  Риск снят сохранением прежней карточки.
- **Командой считается только известное имя**, а не всякая строка с косой чертой: на вопрос «где
  что лежит?» человек отвечает `/opt/challenge`, и прежняя развилка съедала такой ответ.
- **У интервью есть явный выход `/project отмена`.** Прежняя строка обещала, что «любая команда
  прерывает интервью», — это была неправда, и человек оказывался заперт.
- **Деньги интервью попадают не только в токены, но и в рубли сеанса** — та же формула, что у
  пользовательского обмена.
- **Заведён третий файл проверок `myharness/tests/check_layers.py`** — он поднимает настоящую
  точку входа `cli.repl` и шлёт нажатия трубой. Прежние два собирают состояние руками и потому
  по построению не видят бед вида «нужное просто не заводится при запуске» (ровно это в дне 9
  скрыло незаведённое хранилище разговора).

### Просьба пользователя по ходу исполнения (2026-09-14)

Пользователь принёс вопрос ментора — «вы явно выбирали, что и куда сохраняется» — и попросил:
у каждого слоя должны быть все три действия (запись, чтение, удаление), команды должны быть в
`/help`, а дополнение должно подсказывать по пробелу дальше.

Сделано в тот же день:

- **закрыты две дыры**: из карточки нельзя было убрать ни строку, ни карточку целиком, из
  разделов задачи — отдельную строку. Заведены `/task забыть <раздел> <номер>`,
  `/project забыть <раздел> <номер>`, `/project забыть карточку` (с копией рядом);
- **номера строк в показе** задачи и карточки — иначе удалять не по чему;
- **таблица «слой — записать — прочитать — убрать» в `/help`**;
- **подсказки второго и третьего уровня**: после `/task ` — слова команды, после
  `/task забыть ` — разделы, после `/task забыть план ` — номера строк вместе с их текстом;
  то же у `/project`. Состав считает `ui.подсказки` — чистая функция, проверяемая без
  терминала; сверка с раздачей команды стоит проверкой.

**Естественная запись** («запиши в задачи проекта: …», «вот тебе факт обо мне: …») —
**решение пользователя: отдельным днём**, и трогать ей разрешено всё, кроме раздела
«Ограничения». Устройство, которое предложено и согласовано: фраза-зачин («запиши»,
«запомни», «добавь в») запускает один дешёвый запрос, модель возвращает слой, раздел и текст,
программа ПОКАЗЫВАЕТ, куда собирается положить, и кладёт — со способом отменить. Явный выбор из
условия задания при этом сохраняется: решение видно и обратимо. В отложенные.

### Ручной сценарий команд (2026-09-14)

Шаги 6, 7 и 9 пройдены в ЖИВОМ приложении — настоящая точка входа `cli.repl`, поддельный
терминал, подставной клиент: `/task` во всех видах, `/project` с разделом из нескольких слов,
`/memory` с четырьмя слоями, `/system` с точным текстом, закрытие задачи. Все 26 проверок
сценария прошли, включая главную: напечатанное `/system` совпало с тем, что ушло в модель, а
хвост стоит перед вопросом и в памяти агента его нет.

Сценарий стоит сохранить проверкой: он поднимает настоящую точку входа, а прежние проверки
собирают состояние руками и потому мимо неё (ровно этим в дне 9 была пропущена беда с
незаведённым хранилищем разговора).

### Живой прогон влияния слоёв (2026-09-14, deepseek-v4-flash)

Три состава, каждый дважды, признак проверяет программа — не тон и не длина:

| Состав | Вопрос | Признак | Со слоем | Без слоя |
|---|---|---|---|---|
| карточка проекта | «как мне запустить проверки?» | в ответе `uv run` и `check_units` | 2/2 | 0/2 |
| рабочее состояние | «что делаем дальше?» | назван следующий пункт плана — «команды памяти» | 2/2 | 0/2 |
| записи о человеке | «объясни, что такое декоратор» | объяснение через параллель с Kotlin/Java/JVM | 2/2 | 0/2 |

Ни один состав не разошёлся между двумя прогонами, то есть разница держится слоями, а не
случайностью выборки. Без карточки модель уходит в общие советы («смотри scripts в package.json»),
без рабочего слоя прямо говорит, что контекста нет, и просит рассказать, на чём остановились.

## Программа недели (слайд ментора «ЦЕЛЬ») — не забывать

Пять целей недели, из них сегодня закрывается вторая с половиной:

| Цель | Когда |
|---|---|
| реализовать Stateful агента | частично сегодня: слои памяти и состояние задачи |
| настроить персонализацию | сегодня: карточка проекта, интервью, две персоны |
| реализовать конечный автомат состояний | отдельным днём; поле `этап` заводится сегодня |
| настроить инварианты | отдельным днём; раздел «Ограничения» заводится сегодня |
| объединить всё в систему | последним днём недели |

Формат файлов подобран так, чтобы третья и четвёртая цели легли в уже заведённые разделы без
переписывания — ровно то, ради чего задание поставлено на неделю вперёд.

## Открытые вопросы

- Разделение записей по областям жизни — см. дизайн, раздел «Открытый вопрос». Решается замером на
  живом прогоне, не рассуждением.

## Найденное попутно, в этот день не чиним

- `context_strategy.py:88` собирает блок фактов через `sorted(values)`: новый ключ, попавший по
  алфавиту в середину, обнуляет кэш поставщика на всём хвосте блока. В отложенные.
- Архивариус плодит почти-дубликаты, потому что не видит уже записанного (отложенная задача №5).
  Лечение дешёвое — показать ему текущий список и просить операции «добавить / заменить N / ничего».

## Вердикт Validation

_не пройдена._
