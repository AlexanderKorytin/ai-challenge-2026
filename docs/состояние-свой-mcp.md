Род: задание дня 17 (неделя 4, день 2, ветка `w4d2`). Ведущая среда: Claude Code.

Настройка сервера до дизайна сделана: ssh challenge → 83.136.235.149, вход только по ключу (`/etc/ssh/sshd_config.d/10-challenge.conf`), git, uv 0.12.17, `/opt/challenge` с git (фиксация 60639ee).

# Состояние: свой MCP-сервер (день 17)

## Стадия
Spec, второй рубеж — идёт (основа c31922c). Executing закрыт: шаги 1–6 зафиксированы в main (cf9364e, 02f90d5, ebb09d2, 27e3778, aa1b98c) и w4d2 (4d450d3).

## Решения пользователя
- 2026-09-22: git и GitHub уже есть (день 16) — сервер вокруг другого API.

## Найдено в разборе
- Спецификация `mcp`: инструменты только перечисляются, передача модели — «следующая работа».
- Агент уже умеет круг инструментов: `agent.Инструменты(схемы, исполнить)`, поставщик один —
  автомат задачи (`state.py:573`, `machine.набор`). Мост MCP ложится вторым источником в тот же
  набор, а не вторым кругом.
- Серверная сторона есть в установленной `mcp` 2.2 (`mcp.server.mcpserver.MCPServer`, `@tool`).

- 2026-09-22: API — курсы ЦБ РФ (подтверждено повторно после вопроса о погоде).
- 2026-09-22: сервер живёт на VPS 38.180.117.69; способ связи — HTTPS + токен (Caddy, имя sslip.io) — рекомендован, закрепить при утверждении дизайна. Сначала настроить VPS.
- 2026-09-22: ssh к VPS обрывается до обмена ключами. Причина на Маке: трафик идёт через туннель VPN Happ (`utun25`), он принимает любой порт и рвёт. Ждём, что пользователь добавит адрес в исключения Happ или выключит его. Внести в `docs/грабли.md`.
- 2026-09-22: разрешение — список `permissions.allow` из `.claude/settings*.json` рабочего каталога, неразрешённые не предлагаются.
- 2026-09-22: VPS заменён: новый 83.136.235.149 (`koritin84.fvds.ru`), образ FirstVDS с панелью ispmanager (nginx, свой сетевой экран, DNS, MySQL, PHP). Путь в Happ-исключения добавлен пользователем, ключ `challenge_vps` положен им через `ssh-copy-id`.
- 2026-09-22: работать ЧЕРЕЗ ПАНЕЛЬ — решение пользователя вопреки рекомендации (чистая Ubuntu). Риски для отчёта: панель может переписать ручные настройки nginx и экрана; её службы заняли 687 из 956 МБ; часть устройства живёт в панели, а не в git. Правка nginx — только штатными местами панели (`mgrctl`, `vhosts-includes`).
- Профильный разбор не брали: MCP разобран в дне 16, круг инструментов — в дне 13.

## Открытые вопросы
- План `docs/план-свой-mcp.md` согласован. Токен у клиента: не хранится на Маке, берётся при запуске `ssh challenge` из `/opt/challenge/.env` (шаг 6).

## Решения по ходу
- 2026-09-22: у стратегии `branching` инструментов MCP нет (звенья там не хранятся, `memory` «Узел Branching») — решение ведущего по очевидному умолчанию.
- Основа второго рубежа (вход в Executing): c31922c
- 2026-09-22: шаги 1–2 зафиксированы. Открыто: сертификат Let's Encrypt для koritin84.fvds.ru — предел fvds.ru (50/нед. на всех клиентов FirstVDS), панель повторяет; не выйдет — HTTPS по IP-сертификату (пользователь спрашивал «по IP с портом»; голый HTTP отвергнут — токен открытым текстом). Служба запускает .venv/bin/python без uv (отступление от плана ради DynamicUser).
- 2026-09-22: шаги 3–5 зафиксированы (02f90d5, ebb09d2, 27e3778). Сертификат на имя второй раз упёрся в предел (следующая попытка 16:10 UTC); решение пользователя — HTTPS по IP :8443 с IP-сертификатом панели + ежедневная перезагрузка nginx. Добавка IP (ebb09d2) независимо не просмотрена — включить в итоговый просмотр Validation.
- Отступления от плана внутри шагов: в state.py нет условия про branching (главный экран всегда standard, агенты ветвления без поставщика); добавлена проверка живости `ping` перед обменом; успешный результат не вычищается, в isError заменяются только подставленные `${VAR}`.
- 2026-09-22: шаг 6 — живой обмен через VPS (https://83.136.235.149:8443/mcp): модель вызвала get_rate ×2 и get_rate_dynamics, ответ по данным верен. Прогон в интерфейсе терминала — в Validation. Добавка справки /help mcp (aa1b98c) — на итоговый просмотр.
