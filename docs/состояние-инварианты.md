Род работы: задание дня 14 (инварианты и ограничения состояния). Ведущая среда: Claude Code.

## Стадия

Research — закрыта 2026-09-17: **дизайн утверждён пользователем 2026-09-17**. Plan — закрыта: **план `docs/план-инварианты.md` согласован пользователем 2026-09-17** (без предложения признаков моделью). Spec, первый рубеж — 2026-09-17, проверял `general-purpose` на `opus`: 9 находок (главные — блок
первым ломал кэш поставщика и правило порядка системной части; устаревшие оговорки в `memory`,
`agent`, `task-machine`; ошибка `/task забыть ограничения` → `ограничение`) и мелочи; всё закрыто
правкой плана, повторный просмотр — чисто. Решение ведущего: блок «Инварианты» — после карточки,
перед выжимкой (не первым), **место блока согласовано пользователем 2026-09-17.**
Executing — начата 2026-09-17; основа второго рубежа — `96fa28e` (плюс эта фиксация состояния). Дизайн: `docs/инварианты.md` (папка дня ещё не заведена).
Разведка: ограничения есть в «Ограничениях» карточки (замок человека) и задачи; в автомате их нет; судья ответов замышлялся в дне 11, но не написан.

## Устные решения пользователя

- Вопрос пользователя: ограничения сейчас разбросаны (память задачи, глобальная память,
  автомат состояний) — где хранить и что действует при противоречии; предложение — ближайшее
  к задаче.
- 2026-09-17: инварианты живут в четырёх местах — задача, автомат (запреты переходов и ворота),
  глобальная память (пример — запрет транслитерации), плюс карточка папки. Дизайн поправлен.
- 2026-09-17: добавить запретные признаки (подстроки в блоках кода ответа, задаёт человек);
  проверку кода проекта — в отложенную задачу 3 (записано).
- Для ролика нужно задание, которое агент откажется выполнить, объяснив ограничениями.

## Открытые вопросы

- Код идёт в `main` (правка общего инструмента); папка дня и ветка — под постановку и видеоотчёт, как в днях 11–13.
- Заводить ли папку дня `week_3/day_4/` (ветка `w3d4`) — спросить после дизайна.

## Ход исполнения

- Шаги 1 и 4 закрыты фиксацией `1b4cd4f` (вердикт в сообщении).
- Шаг 2 закрыт 2026-09-17 без фиксации кода: живой прогон через `cli._main`, профиль по
  умолчанию, `deepseek-v4-flash`, карточка П1–П4. Запрос «Go + Firebase» → «Инварианты: запрос
  нарушает П1, П2, П3, П4», разбор по каждому, решение в рамках, «снять может только человек
  командой». «Напоминания за сутки» → «Инварианты: нарушений нет.» (с точкой — `строка_сверки`
  должна это принимать). Скрипт прогона — в песочнице ведущего (`scratchpad/live/run.py`),
  перенести в `week_3/day_4/test/` при заведении ветки дня.
- Шаг 3 закрыт фиксацией `a3c30b5`. Договор проверки уточнён на круге (подпись поставщика
  `(вопрос, ответ, *, судить, run_id)`, поля `отброшены`, `инвариантов`, `run_id`) — записано в план.
- Шаг 5 закрыт: `openspec/specs/invariants/` (ведущий) и правки `memory`, `agent`, `task-machine`,
  `profiles`, `commands`, `journal`, `tokens`, `interface` (исполнитель `general-purpose`/opus);
  validate — 17 из 17. Открытые мелочи для второго рубежа: столбец «убрать» справки `/help` у
  инвариантов короче, чем в требовании `commands`; довод «профиль пишет модель» в `overrides`.
- Executing закрыта 2026-09-17 → Spec, второй рубеж; основа — `0aa2707`.
- Spec, второй рубеж 2026-09-17 (проверял `general-purpose`/opus): 2 находки — порог сжатия в `agent`
  не называл карточку и блок инвариантов; `overrides` на запись-инвариант молча не действовал
  против «всё отброшенное называть вслух» у мастера профиля — и мелочи (слово «пометка», `sliding`
  без фактов, `/project забыть`, исключение `судить`, сверка номеров со списком `Г/П/З`, сценарий
  журнала). Вторая находка потребовала кода → возврат в Executing, основа `569b3a9`: мастер
  отбрасывает запись-инвариант из перекрытий вслух, подсказка `/invariants` называет
  `/project забыть Ограничения N`. Решение ведущего: правка `interface` (вес — карточка и блок
  инвариантов) принята, хотя план говорил «не меняется»: она согласует `interface` с `agent`/`tokens`.
- Возврат в Executing закрыт: мастер читает записи одним снимком (`load_records`) под прежней
  защитой и передаёт `Интервью.инварианты`; просмотрел тот же проверяющий — чисто.
- **Spec, второй рубеж — чисто** 2026-09-17 (после круга: ключ `отброшены` в `journal`).
  Следующая стадия — Validation (`/проверка`). Проверки на момент выхода: check_units, check_layers,
  check_app — зелёные; `openspec validate --specs --strict` — 17 из 17.

## Validation

- Итоговый просмотр связности 2026-09-17 (`general-purpose`/opus, новый проверяющий), срез
  `0aa2707..406f78a`: 2 замечания — (1) при Sticky Facts факты применялись до судьи и оседали при
  отмене посреди него; (2) строка из одного хвоста признаков сдвигала номера `П/З` против номера
  удаления. Validation → Executing (основа `406f78a`): факты применяются после проверки; номер —
  место строки. Проверка отмены падала на прежнем `agent.py` и проходит на новом. Повторный
  просмотр того же проверяющего (расширен до всего пути facts) — чисто; он же сверил изменённые
  требования `invariants` (нумерация) и `agent` (порядок фактов) с кодом — это и есть повторный
  второй рубеж Spec для этих двух положений. → Validation, полный проход.
- **Полный проход 2026-09-17 на неподвижном дереве `38e57dc`:**
  - `uv run python tests/check_units.py` — код 0, 1895 OK, «Все проверки пройдены»;
  - `uv run python tests/check_app.py` — код 0, 443 OK, «Все проверки пройдены»;
  - `uv run python tests/check_layers.py` — код 0, 138 OK, «Сценарий пройден целиком»;
  - `openspec validate --specs --strict` — код 0, 17 из 17;
  - зеркала помощников не задеты — `sync_agents --check` не нужен.
- **Живой сценарий ролика** (`deepseek-v4-flash`, профиль «архитектор» с картой RESEARCH→PLAN→
  EXECUTING→DONE, через `cli._main`; скрипт `scratchpad/live/scenario.py`, перенести в
  `week_3/day_4/test/`): 10 инвариантов Г1, П1–П4, З1–З2, А1–А3.
  - напоминания → «нарушений нет»; Go+Firebase → «нарушает П1, П2, П3, П4»; уговор → тот же отказ;
    Dagger «для сравнения» → «нарушает З1», код только на Koin; «пропусти согласование» →
    «нарушает А1, А2»; З3 «копия в Google Drive» против П3 → модель назвала оба и спросила, какой снять.
  - Судья звался 6 раз, общий `run_id` с главной записью, ложных тревог на отказах нет.
  - **Не подтверждено живьём:** поимка признака (модель ни разу не выдала код на Dagger) и
    вердикт судьи «нарушено» (модель ни разу не нарушила). Доказаны только подставным клиентом.
  - **Найдено слабое место:** Г1 (транслитерация) не удержан — на «распиши, как задеплоим и
    закоммитим» модель сослалась на А1/А2, а в ответе сама написала «разбивка на коммиты»; судья
    нарушения не увидел. Признаки у Г1 не заданы, а прозу признаки не проверяют по замыслу.
- **Расход дня** (`python3 bin/расход.py --день 2026-09-17`): ведущий 150 обменов, помощники 323;
  вход 71,4 млн (ведущий 34,5, помощники 36,9), выход 174 тыс., 147 $, 2 ч 04 мин.
- Вердикт Validation: результат подтверждён проверками и живым сценарием, кроме названного выше.
  Ждёт подтверждения пользователя → Report.
- **Результат подтверждён пользователем 2026-09-17** → Report.

## Report

- 2026-09-17: ветка `w3d4`, папка `week_3/day_4/` (README, `test/запуск.sh`, профиль «архитектор»,
  `test/живой_сценарий.py`); состояние показа — только в `test/state/`. Ролик 3 мин 42 с:
  https://drive.google.com/open?id=1gzJPw55uun7IjB-bnFfnbD-p38IbpBRS; код дня —
  https://github.com/AlexanderKorytin/ai-challenge-2026/tree/74ce8961ca08b95c1dd751f7f55ebac72e90c179/week_3/day_4
- В навык `/отчет` внесены три правила записи (весь экран, сверка окна, пауза после вставки).
- Замечено при записи: при неподвижном экране файл `ffmpeg` по ходу не растёт — данные
  сбрасываются крупными кусками; признак «растущий файл» в навыке ненадёжен, рабочая проверка —
  короткий пробный захват с `-t 4` и кадр из него.
- **Done.**
