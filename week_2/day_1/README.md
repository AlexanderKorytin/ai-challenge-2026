# День 6 — Первый агент

Ветка: `w2d1`. Стадия: **Report**.

## Постановка

> Реализуйте простого агента, который принимает запрос пользователя, отправляет его в LLM через
> API, получает ответ, выводит результат в вашем интерфейсе.
>
> Важно: агент должен быть **отдельной сущностью**, а не просто одним вызовом API; логика запроса
> и ответа должна быть **инкапсулирована в агенте**.
>
> Результат: агент принимает запрос и корректно вызывает LLM через API. Формат: видео + код.

Уточнения из переписки с ментором:

* сущность держит системную инструкцию, параметры, **складывание стека сообщений** и пересылку в
  LLM; наружу отдаёт только результат;
* это «коробка» со своей политикой входа, политикой выхода и, если нужен, своим судьёй —
  изолированный модуль внутри приложения;
* устройство обязано выдерживать сто агентов с разными настройками: **один процесс приложения,
  сто экземпляров агента внутри**.

Дополнительно от пользователя: режим, в котором инструмент не ждёт человека в цикле, а сразу
исполняет то, что записано в конфигурации.

## Ход работы по стадиям

### Research — завершено

Классификация: архитектурная задача. Опрошены три профильных агента (устройство сущности,
управление памятью, путь перестройки), их расхождения разобраны и решены явно. Разведка по сети
о способах проверки агентов. Итог — [`дизайн.md`](дизайн.md), утверждён пользователем.

Ключевые решения: клиент API — аргумент вызова, а не поле агента; журнал пишет агент через
развязанный `journal.append`; составные режимы — оркестраторы над множеством агентов; потоковый
вывод через функцию обратного вызова, а не асинхронный генератор; память — скользящее окно с
блочной обрезкой, без пересказа.

### Plan — завершено

[`план.md`](план.md): 60 шагов, двенадцать блоков (А–М), ~7 ч 45 мин. Три рубежа: шаг 25 —
условие дня закрыто, шаг 39 — закрыта изоляция модуля, шаг 53 — закрыт режим без человека
в цикле. Разделение проверок записано в план: строгий цикл на логику, ручной прогон на
текстовый интерфейс.

Найдено при планировании: `__init__.py` — это `from .cli import main`, поэтому любой импорт
пакета тянет `prompt_toolkit`; пакетный режим без терминала требует отдельной лёгкой точки
входа (`args.py`).

### Executing — завершено

Двенадцать блоков плана, каждый — отдельный исполнитель, каждый закончен фиксацией с зелёными
проверками. Исходный слепок до правок: 147 утверждений — [`test/до.txt`](test/до.txt).

| | утром | вечером |
|---|---|---|
| проверок | 147 | **257** |
| `cli.py` | 1181 строка | 983 |
| логика разговора | три функции и поле панели | `agent.py`, 300 строк |
| история разговора | внутри `Pane` — элемента интерфейса | внутри агента |
| сборок запроса | две, расходящиеся побочным действием | одна |
| скрытых круговых зависимостей | 6 ленивых импортов | **0** |
| способностей в требованиях | 10 | **12** |

**Что появилось**: `agent.py` (сущность), `output.py` (вывод), `batch.py` (пакетный наряд),
`args.py` (лёгкий разбор ключей). **Что исчезло**: `cli.generate_response`,
`cli.build_request_messages`, `screens.build_messages`, поле `Pane.messages`.

**Возможности сверх перестройки**: окно памяти со срезом блоками; режим `--batch` без человека
в цикле; список запущенных агентов по `/agents` со временем и токенами.

#### Поправки плана по ходу работы

План правился шесть раз, и каждый раз — потому что работа вскрывала то, чего не видела разведка.
Записаны прямо в [`план.md`](план.md), рядом с затронутым шагом:

1. **Шаг 4 перенесён к шагу 16.** Проверка, написанная за двенадцать шагов до своего кода,
   держала бы набор красным всё это время — то есть перестройка шла бы без страховки.
2. **Мест сборки сообщений оказалось четыре, а не три.** Разведка пропустила ветку рабочего
   экрана в `cli.worker`. Без правки рабочие экраны тихо перестали бы копить историю, **а все
   проверки остались бы зелёными**: они ходят по главному экрану.
3. **Переходник из шага 25 оказался неисполним.** Замысел «оставить старую функцию, а внутри
   создавать одноразового агента» не сходится с тем, как устроены вызывающие. Порядок блоков
   перестроен: агенты сначала заводятся на панелях, потом все вызовы переключаются разом.
4. **Шаг 46 перенесён в блок З.** Проверка изоляции агента неисполнима, пока `__init__.py` —
   это `from .cli import main`: любой импорт пакета тянет весь интерфейс.
5. **Критерий развязки уточнён.** «Ноль ленивых импортов по всему каталогу» противоречил
   шагу 46: в оркестраторах такой импорт разрывает круг, в точке входа — экономит вес.
6. **Добавлен блок Н** — список агентов, по просьбе пользователя посреди дня.

#### Находки, которых не ждали

- **Живой прогон наряда поймал настоящий сбой**: задание израсходовало весь `max_tokens` на
  рассуждения и не начало ответ — 1412 токенов заплачено, ответа нет. До сегодняшнего дня это
  был бы прогон со статусом «успех» и пустой строкой. Поймала политика выхода, заведённая утром.
- **Строгий цикл поймал ошибку в свежем коде**: на промежуточной версии `exchange` пара
  «вопрос — пустой ответ» оседала в памяти агента и уехала бы в следующий запрос.
- **Панели выбора не были кликабельными вовсе** — клик умели только вкладки. Починено попутно,
  мышью заработали `/profile`, `/params`, `/set`, `/model`.
- **Гипотеза дизайна не подтвердилась** — см. [`test/замер.md`](test/замер.md).

### Validation — завершено

Прогон обоих наборов целиком, сверка с требованиями, просмотр ветки отдельным проверяющим,
мутационная проверка самих проверок, живые прогоны.

| Проверка | Результат |
|---|---|
| `check_units.py` | 202 утверждения, зелёные |
| `check_app.py` | 101 утверждение, зелёные |
| строк «СБОЙ» | **0** |
| `openspec validate --specs --strict` | 12 из 12 |
| следы удалённого кода | не найдено |
| `ruff` | 44 замечания, все унаследованные |

#### Найдено проверкой и исправлено

1. **`/clear` посреди идущего запроса не чистил память и врал, что почистил.** Команды
   исполняются мимо очереди, обмен доходил до конца и клал пару обратно. Поломка родилась на
   стыке: команды шли мимо очереди всегда, память переехала в агента сегодня — по отдельности
   ни один блок ошибки не содержал. Лечение: счётчик поколений памяти.
2. **Ведущий группы начал копить историю.** Раньше сводка собиралась вручную и была разовым
   запросом; после переноса на агента унаследовала `keep_history` из профиля. Решения об этом
   никто не принимал — поведение и цена изменились сами собой. Типичная цена перестройки: не то,
   что сломалось с грохотом, а то, что тихо стало другим.
3. **Повтор имени в составе группы или наборе способов** слал два обмена одному агенту —
   двойные деньги за опечатку в профиле.
4. Мёртвые параметры от старого пути; две разные функции подсчёта токенов вместо одной.
5. **Семь дыр в самих проверках**, найденных мутациями: код ломался по одному месту, и набор
   оставался зелёным. Число утверждений — не мера качества; мера — что они ловят.

#### Осталось непроверенным

- **Живое поведение сети**: обрывы, пределы частоты, отказы поставщика. Проверено только то, что
  прогнали: два наряда и один разговор.
- **Умолчание одновременности (8)** обосновано рассуждением, а не замером против настоящего
  предела DeepSeek.
- **Настоящий терминал**: отрисовка, счётчик ожидания, мышь у терминала — всё гонялось на трубе
  и пустом выводе.
- **Смена профиля во время идущего запроса**: память больше не портится, но ответ старого агента
  садится в ленту после сообщения о смене — читается так, будто ответил новый.

#### Риски, принятые сознательно

- **Копия профиля ведущего неглубокая**: `params` и `vars` — те же словари, что у профиля
  пользователя, а `system` скопирован значением. Значит `/set` достаёт агента сводки, а правка
  инструкции — нет: половина профиля живая, половина замороженная. Через интерфейс инструкцию
  присвоить нечем (`/system` только показывает), поэтому изъян недостижим. Решение принято
  пользователем: оставить и записать, а не править работающий код ради недостижимого случая.
  Лечение, если понадобится, — пересобирать профиль сводки на каждом прогоне группы, а не один
  раз при создании экрана.
- **`history_window` — нижняя граница, а не точное число**: верхняя равна ей плюс пять пар.
  Название вводит в заблуждение при маленьких окнах; записано в требования вместо переделки,
  потому что поведение верное, врёт только имя.
- **Ветка «запущенных агентов нет» недостижима**: собеседник главного экрана есть всегда.
- **Унаследованное, не трогали**: `--model` молча оседает в файле настроек; `Screen.is_agent` и
  `State.log` мертвы; 35 из 44 замечаний `ruff` — устаревшие пометки в старых файлах.

### Report

_в работе_

## Отложено на следующий день

**Возврат итога оркестратора в главный экран.** Сегодня цепочка и группа оставляют результат на
своих вкладках, а главный экран показывает только «способы: …» или «группа поднята: …». Итог в
главный разговор не возвращается — то есть человек, ведущий беседу на главном экране, обязан сам
пойти и посмотреть.

Пробел настоящий, найден пользователем при разборе демонстрации. Решение пользователя — отложить:
работа хорошо ложится на тему обратной связи и цикла «поставил задачу → проверил → исправил»,
которая и так впереди.

Что понадобится: оркестратор по завершении кладёт итоговый ответ в главный экран с пометкой, кто
его дал; для группы это сводка ведущего, для цепочки — ответ последнего шага. Плюс требование в
`openspec/specs/methods/` и `openspec/specs/team/`.

### Validation

### Report
